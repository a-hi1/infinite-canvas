---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- AI 接口安全代理后续：已增加首版 `ai-proxy` 服务和前端代理渠道入口，后续继续完善多用户鉴权、额度控制、分渠道上游配置和更细粒度的接口白名单体验；公开多人使用前仍需补充更完整的用户体系。
- **当前主线（收费后置）：** 本地 BYOK 稳定 + 云历史双轨可回归 + 画布项目 JSON 云同步（P2 MVP）+ 上游能力按矩阵切片。完整矩阵见 [上游跟进矩阵](/docs/progress/upstream-follow)。
- 云端账号 P0.5c：清单 [P0.5c 验稳收口](/docs/progress/p05c-acceptance)。自动：`scripts/check-cloud-stack.*` + `scripts/smoke-dual-track.*`。仍须人工：登录上云 UI、跨用户、服务器 B2。**勿跳过验收直接支付/全量同步。**
- 云端账号 P1：底座 A～G 代码已在（网关+账本默认关）。**画布扣积分 / 平台扣积分 / 支付均后置**（扣积分依赖平台计费开关，不应先于画布上云）。
- 云端账号 P2：**P2.0-A 画布 JSON** + **P2.0-B 画布媒体 blob** + **P2.0-C 素材库清单云同步** 已完成——`/api/projects`、`/api/blobs`、`GET/PUT /api/assets`（用户级 manifest + tombstones 防删除复活）；素材/画布媒体共用 blob 同步；本地优先、云失败不丢稿。**未做：** Postgres、S3、素材跨用户分享。画布/素材扣积分仍后置。
- 上游跟进：对照约 `v0.9.0`；**不整仓 merge**；推送 `a-hi1`。已移植：透明背景（BYOK）、组节点（最小）、模型调用脚本（旁路 modelScripts）、**导出当前画布/选中节点（侧栏能力安全切片）**。未移植：完整可调宽侧栏/资产卡重做（依赖插件 registry，后置）。下一优先候选：P0.5c 人工 UI 验收收口 →（独立工程）插件系统。
