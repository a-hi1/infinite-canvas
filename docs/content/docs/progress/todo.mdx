---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- AI 接口安全代理后续：已增加首版 `ai-proxy` 服务和前端代理渠道入口，后续继续完善多用户鉴权、额度控制、分渠道上游配置和更细粒度的接口白名单体验；公开多人使用前仍需补充更完整的用户体系。
- **当前主线（收费后置）：** 本地 BYOK 稳定 + 云历史双轨可回归 + 上游能力按矩阵切片。完整矩阵见 [上游跟进矩阵](/docs/progress/upstream-follow)。
- 云端账号 P0.5c：清单 [P0.5c 验稳收口](/docs/progress/p05c-acceptance)。自动：`scripts/check-cloud-stack.*` + `scripts/smoke-dual-track.*`。仍须人工：登录上云 UI、跨用户、服务器 B2。**勿跳过验收直接支付/全量同步。**
- 云端账号 P1：底座 A～G 代码已在（网关+账本默认关）。**平台扣积分与支付产品面后置。** 需要收费时再：开 `API_PLATFORM_*` → 支付入账 → 再考虑视频参考图/画布走平台额度（禁止一次合并）。
- 云端账号 P2：画布/素材云同步；JSON→Postgres；文件→S3/MinIO。先本地后异步推云。与插件大改错开。
- 上游跟进：对照约 `v0.9.0`；**不整仓 merge**；推送 `a-hi1`。已移植：透明背景（BYOK）。下一优先候选：**组节点** → 模型调用脚本 →（独立工程）插件系统。
