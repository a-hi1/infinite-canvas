---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- Grok 完成无播放地址：`status=done` / `progress=100` 但 `video.url` 空串或晚写时，轮询再等约 3 分钟并尝试 `/videos/{id}/content` 等下载；错误文案含 `video.url` 形态与 `respect_moderation` 提示，**不改** 创建路径/多参考 payload。需验证：① 正常出片仍可播；② 若中转晚写 url，不再约 1 分钟就失败；③ 若 url 始终为空，失败信息可读且含诊断；④ 多参考/1080 降档/云同步无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 视频结果真实清晰度标注：生成成功后若实测像素低于所选（如选 1080p 实为 720p），结果卡与历史记录显示「实 720p · 选 1080p」，不再只贴所选档位；仍保留 toast 提示。**不改** Grok 请求顺序/降档逻辑/生成路径。需验证：① 选 1080 实际 720 时结果区与历史有「实」标签；② 真 1080 只显示正常档位无橙标；③ 生成/Grok 多参考/云同步无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 版本更新弹窗双源：默认展示**本机** `CHANGELOG.md`（构建时 `__APP_RELEASES__`，含 Unreleased 自用增量）；**上游** basketikun 日志单独折叠，点击展开对照；打开弹窗/「检查更新」只写入上游分区，**不再** `setReleases` 覆盖本机列表。右上角绿点仍表示上游 VERSION 高于本机。需验证：① 打开弹窗立刻见「本机更新」含工作台 `@`/九宫格/工作空间等条目；② 上游区默认折叠，展开后有上游版本时间线；③ 点「检查更新」本机列表不变；④ 绿点/检查失败不崩；⑤ 生成/登录/配置无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 图/视频**工作台**提示词 `@` 选择参考素材：复用画布 `CanvasPromptChipInput` + adapter `workbench-prompt-references`；仅可选择**已添加**参考（上传/拖拽/剪贴板/资产不变）。图片 label=`图片N`；视频含图/视/音，label 与参考条/`seedanceReferenceLabel` 一致。序列化为纯文本 label，**不改** `requestEdit` / `createVideoGenerationTask` / Grok 多参考 payload。无参考时 `@` 无菜单；AI 优化中禁用输入。需验证：① 图片工作台上传 2 张后 `@` 见缩略图并插入 chip；② 生成请求 prompt 含 `图片1` 无 HTML；③ Ctrl+V 截图仍进参考条；④ 提示词库/历史回填后 chip 重建；⑤ 视频页图+视+音菜单 label 正确且 payload 三数组不变；⑥ 画布节点 `@` 无回归；⑦ Grok 多图 Network 仍全量 refs。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布九宫格 / 角色表轻量切片（**不新开 App**，挂既有入口）：① **AI 优化**自动识别 `九宫格/角色表/表情表/character sheet/3×3` → intent `character_sheet`（更长字数 + 单张 3×3 同一人手册）；普通「女主定妆」仍走人物手册。② 图片节点 **切图**对话框新增「角色九宫格 3×3」预设（默认仍 2×2）；生成子节点写 `metadata.sheetCell`（row/col/label）+ `sourceSheetNodeId`，3×3 另写 `sheetPick: null`。**不改** 图/视频生成请求路径、Grok 多参考、工作台。需验证：① 提示词含「九宫格表情表」点 AI 优化，结果更像单张 3×3 角色表而非分镜故事；② 「女主半身定妆」优化仍像单人像；③ 切图点 3×3 → 9 个子节点标题 `… 1-1`…`3-3`，节点 JSON 有 sheetCell；④ 默认切图仍 2×2 可用；⑤ 生成/存资产/发布空间无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作空间文件夹式归纳（素材墙 + 生成分享）+ **UI 去杂乱**：条目独立 `folder`（与 `category` 正交，max 80，空=未归入）；API create/list/update/public 读写；list 支持 `folder=` 与 `__unfiled__`。**呈现分层（不删功能）：** ① 素材墙上传旁「放入：未归入/名」紧凑目标（可新建）；② 墙顶「批次文件夹」chips **仅具体文件夹 + 未归入**（无「全部」chip；默认不选=全量；再点可取消/有「清除」）；无文件夹时弱提示 + 分割线后「内容分类」+ 终稿，计数并入文件夹行；③ **未勾选**批量条只保留全选 +「勾选后可批量整理」+ 智能归类链；**勾选后**分组展开 分类/文件夹/终稿/存删。分享弹窗可选批次文件夹。智能归类**只动分类**。**不改** 进度板/决议/私有资产/生成路径/API 语义。需验证：① 上传到「定妆」后只筛该文件夹可见；② 分类 chips 仍可再筛；③ 生成分享批量移入后文件夹条出现；④ 分享资产指定文件夹进素材墙；⑤ 未设文件夹在「未归入」，「全部」见所有；⑥ 智能归类不改 folder；⑦ 成员不能改他人 folder（非 owner）；⑧ 旧条目无 folder 不报错；⑨ **未勾选时无整排 disabled 批量按钮，勾选后分类/移入/终稿/存/删均可用**；⑩ **无文件夹时墙面更干净，上传带文件夹后 chips 出现**；⑪ 终稿/决议/删除回归。**Docker 需 `up -d --build app api` 后硬刷新。**
- 工作空间墙批量操作 + 智能归类：素材墙 / 生成分享对齐「我的资产」——卡片左上角勾选、全选当前筛选、批量设分类 / 标终稿 / 取消终稿 / 存到我的资产 / 删除选中；「智能归类未分类」对当前筛选中无分类且可编辑的条目按标题/提示词/备注推断；单卡「快速设分类」下拉。权限仍为创建者或所有者可改/删；分享入库时已有自动分类不变。**不改** 生成/私有同步/进度板/决议 API。需验证：① 分享后有分类或未分类符合推断；② 勾选多卡批量设分类后筛选命中；③ 智能归类只动可编辑未分类项，证据不足仍未分类；④ 批量终稿/取消/存资产/删除权限正确（成员不能删他人）；⑤ 单卡详情/决议/下载仍可用；⑥ 生成路径无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 分享到工作空间加速 + 空间详情再进更快：分享弹窗会话缓存空间列表、默认最近空间；多条分享改为最多 3 路并发 `mapPool` 上传（带进度文案），成功后写入最近空间并失效 items 缓存。`workspace-api` 对 list/detail/items/tasks 增加短 TTL 会话缓存与 inflight 去重；详情页缓存首屏 + 首屏缩略图预取；手动刷新 `force`。**不改** 分享语义/生成路径/权限。需验证：① 资产/工作台/画布分享多张明显更快；② 弹窗默认上次空间；③ 分享后进空间可见新条目；④ 再次进入空间首屏几乎瞬时（缓存期内）；⑤ 点刷新能拿到最新；⑥ 生成/私有同步无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作空间素材直达图/视频/画布：资产选择弹窗（画布「我的资产」、图/视频工作台「我的资产」）增加 **工作空间** Tab。登录后默认选最近空间，可切换；列表图/视/文/文档（文档与文本导入为提示词/文本节点）；点选后经 `workspaceFileObjectUrl` 下载并写入本机 storage 再走原有 `InsertAssetPayload` 插入路径，**不改** 生成请求、Grok/Sora/Veo/Seedance 上限逻辑、分享到工作空间反向路径。需验证：① 图片工作台从空间导入参考图；② 视频工作台导入图/视频；③ 画布插入图/视/文本节点；④ 未登录工作空间 Tab 引导登录；⑤ 我的资产 Tab 行为不变；⑥ 生成路径无回归。**Docker 需 `up -d --build app` 后硬刷新。**
- 协作工作空间 MVP（含本地上传/详情/多负责人/进度色块/多交付物/画布发布/分类墙/终稿/改名/决议/踢人/**文档素材**）：顶栏「工作空间」→ `/workspace` / `/workspace/:id`；列表/详情页与「我的资产」同款点状背景。`api` 独立表/路由（`/api/workspaces*`、`/api/workspace-files/:id`），与私有 `/assets` `/jobs` `/projects` 分轨。**素材墙**（资产分享 + **本地上传图片/视频/文档**，Tab 内「去我的资产选择分享」）vs **生成分享**（Tab 内「去图片/视频工作台分享」）分 Tab；卡片显示上传者；**点击打开详情**；模型展示去掉 `channelId::` 前缀。**文档素材 `asset_document`**：本地上传 `.md`/`.txt`/`.csv`（约 2MB UTF-8）；全文走 `file_id`，卡面仅摘要；详情只读预览 Markdown / 纯文本 / CSV 表（客户端渲染，无协同编辑/PDF）；可下载；「存到我的资产」落为文本资产。**分类墙**：复用资产 taxonomy chips；**详情可手动改分类分区**；选中态浅蓝描边+浅底；**自动分类**：本地上传按文件名推断；分享弹窗（资产/画布/工作台本机与云端历史）无分类时按标题/提示词/标签推断。**终稿好找**：Tab 标终稿数、「仅终稿」筛选、终稿置顶、卡片琥珀描边+角标。**版本/终稿/改名**：详情可改名称/分类/版本号/关联替代/终稿（`PATCH /items/:id`）；终稿琥珀色醒目标识。**卡面不展示提示词**，仅详情可见；详情侧栏分区+底栏动作固定。**团队决议**：详情「用 / 改 / 弃」+ 可选短评（`PUT/DELETE .../items/:id/reaction`，每人一票可改可撤）；卡片角标汇总票数；所有者可清他人决议。**踢人**：所有者在成员 Tab「移除」→ `DELETE /members/:userId`（不能踢 owner；任务负责人会 scrub）。**进度板**：三列色块看板 + 顶部进度条；**拖拽任务左侧手柄跨列改状态**（创建者/所有者，下拉仍可用）；新建/编辑可 **多选成员负责人**；创建者/所有者/负责人可 **追加多个交付物**（图片/视频，单任务最多 12 个，不覆盖旧文件；可单条删除或清空全部；计入上传者容量，经 workspace-files 鉴权）。**画布一键发布**：节点悬停「发布空间」、多选栏/右键/顶栏菜单「发布选中到空间」、侧栏多选「发布空间」→ 复用分享弹窗；图片/视频→`gen_*`，文本→`asset_text`，`source_type=canvas`；空配置/组/无内容节点跳过；未登录弹登录。**账号弹层**：工作空间页打开账号不再整页闪烁（`refreshUsage` 保持 user 引用稳定；列表/详情只依赖 `user.id`）。**不改** BYOK 生成、私有云同步、Grok/Sora/Veo/Seedance/Agnes、导出/存资产。需验证：① 未登录引导登录；② 列表/详情有点状背景；③ 素材墙按钮跳转 `/assets`，生成分享按钮跳转 `/image` `/video`；④ 分享卡片/详情模型名无 `::` 前缀编码；⑤ 进度板三列色块与进度条随状态变化；⑥ 负责人可连续上传多个交付物，网格缩略预览与点击放大/播放可用，单条删除不影响其它，非负责人可预览/下载；⑦ 本地上传图片/视频/md/txt/csv 与多负责人/详情预览仍可用；⑧ B 不能删 A 的分享；⑨ **画布**有图/视频/文本节点可发布到空间且来源显示「画布」，空节点有警告，不自动同步整项目；⑩ 素材墙 chips 可筛人物/场景等，**选中非纯黑、可读**，卡片显示分类/版本/**醒目终稿**/决议角标；⑪ 详情可**改名称**、设终稿/版本/关联替代，**取消关联一键可点**，刷新后仍在；⑫ 成员可投 用/改/弃 + 短评，换票/取消生效，卡片票数更新；所有者可清他人决议；⑬ 所有者可踢成员，被踢后无法再进空间；⑭ **工作空间页点开账号弹层不整页刷新/频闪**，容量数字可正常刷新；⑮ **文档**：上传剧本 `.md` 卡面见摘要、详情 Markdown 可读、可下载、存到我的资产为文本；`.csv` 详情表格预览；超过 2MB/非 UTF-8 被拒；⑯ 回归私有同步与生成路径。**Docker 需 `up -d --build app api` 后硬刷新。**
- 我的资产详情可改名称：资产详情抽屉直接编辑标题（失焦/回车/保存），走 `updateAsset` + 云 manifest 同步；完整「编辑」弹窗仍可用。需验证：① 点开详情改名后卡片标题更新；② 刷新后名称仍在；③ 登录云同步后另一端可见；④ 原编辑弹窗改标题仍可用；⑤ 分类/分享/生成路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 图/视频**工作台**并发生成 + 立即新建左侧任务：点「开始生成」立刻在历史列表插入「生成中」记录（快照当前提示词/参考图），不再等整批结束才出现；生成按钮**去掉 antd `loading={running}` 锁**（loading 会吞点击），进行中文案为「再生成一条」，可改参考图/提示词再点并再建新任务。图片用 `activeImageJobsRef` + 同一 log id 收尾；视频 BYOK 仍早建 log，平台路径也先插「生成中」再 finalize；结果区只跟随最新 job。**不改** Grok 多参考/Sora/Veo/Seedance/Agnes 请求路径、bulk+serial、云同步。需验证：① 图片工作台点生成 → 历史立刻有「生成中」；② 生成中改参考图再点 → 左侧**又多一条**新任务，旧任务继续，按钮可点；③ 两条都完成后各自成功/失败状态正确；④ 视频工作台同①②③；⑤ 点历史旧任务预览仍只回填表单，不会把下一次生成绑到旧 id；⑥ 平台积分路径（若开启）同样立即有任务。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作台终止生成：图/视频工作台生成中可按任务停止。入口：① 生成按钮旁红色「停止」（停最新任务）；② 本机历史卡片「生成中」上的「停止」。实现：每任务 `AbortController`，图片 bulk/serial/edits/platform 与视频 create/poll/platform 均传 `signal`；中止后该条标失败「已停止生成」，**不**影响其它并发任务，也不锁「再生成一条」。刷新后残留「生成中」点停止会本地标记结束（上游任务若已创建则无法远程撤销）。需验证：① 图片生成中点停止 → 该任务失败且请求中断；② 两条并发生成时只停一条，另一条继续；③ 视频轮询中停止不再继续 poll；④ 停止后仍可立即再生成；⑤ Grok/Sora/Veo/Seedance/Agnes 正常完成路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作台云端历史点击预览：`CloudHistoryPanel` 图片用 antd `Image` 点击放大预览；视频点缩略图打开 Modal 大屏播放（卡片内仍可看静音预览帧）。下载/删除/刷新/列表请求不变。需验证：① 图片工作台切「云端」点缩略图可放大；② 视频工作台云端点缩略图可大屏播放；③ 下载/删除仍可用；④ 本机历史预览与生成路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 资产与工作台云端批量操作：①「我的资产」卡片左上角可勾选，工具条支持全选当前筛选 / 导出选中 / 批量设分类 / 批量删除 / 清空选择；批量删除走 `removeAssets`（一次 cleanup + 逐条 tombstone），单卡打开/编辑/下载/分类仍可用。② 图/视频工作台云端历史面板支持勾选、全选当前列表、下载选中、删除选中（串行 `deleteCloudJob` / 下载，确认后刷新列表与用量）；单卡预览/下载/删除保留。需验证：① 资产页勾选多卡可导出 zip、改分类、删除后列表与云状态正确；② 未勾选时工具条按钮禁用；③ 单卡删除/编辑不受影响；④ 工作台云端勾选多条可批量下载与删除，本机历史不受影响；⑤ 云端点击预览仍可用；⑥ 生成/上云路径不变。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布并发生成：同一配置/节点提示词面板可在前一次图/视/音/文生成未完成时改词再点生成；每次点击立即新建 loading 子节点并进入左侧节点列表，生成按钮不锁成「停止」（停止只在结果节点 `isRunning` 时出现）。`runningNodeIds: Set` 只标记结果目标；`runId` 区分批次；同 origin 子节点按已有连线数 + 进行中槽位纵向错位，完成后再次生成也不会盖住旧结果。已有结果节点再生成：有 content/storageKey 一律新建子节点，不原地覆盖；参考图优先当前连线/@ 引用，无连线才回退自身图生图。配置节点无 `@` token 时仍附带已连接媒体。重试同目标仍 abort 旧请求。需验证：① 配置节点连点两次 → 两个 loading + 左侧两条，按钮仍可再点；② 生成完成后改参考图/连线再点生成 → **新建** loading 任务且请求用新参考；③ 改提示词后第二次用新词；④ 点 loading 结果停止只中断该批；⑤ 空图/空视频原地生成与蒙版/角度/重试不受影响；⑥ 批量 count>1 仍正常。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布编辑器小修（v0.11）：组装提示词浮层 `max-h-72 overscroll-contain` 限高滚动；配置节点重试优先 `composerContent` 再展开，避免已展开 `prompt` 再拼上游文本导致组装词重复；切换生成类型时 `resolveCanvasModeModel` 仍按能力回填模型。需验证：① 组装提示词很长时可滚且不撑破浮层；② 配置节点生成后点子节点重试，提示词不重复两遍；③ 配置节点在图/文/视/音之间切换，模型落到对应能力默认；④ 图/视频/音频生成路径不变。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布文本「推理强度」reasoningEffort（v0.11）：文本节点面板与配置节点文本模式新增「推理 · 档位」设置 popover（自动/低/中/高/极高）；写入节点 `metadata.reasoningEffort` 与全局 `AiConfig.reasoningEffort`（默认 `auto`）。默认 OpenAI Responses 路径：`auto` 不传字段，其它档位发 `reasoning: { effort }`；自定义文本脚本可读取 `reasoningEffort` 变量（OpenAI 模板已示例）。生成/重试会把 model + reasoningEffort 快照进文本节点。**不改**图/视频/音频请求。需验证：① 文本节点可见推理设置且可改档；② 配置节点切到文本模式有同款设置；③ 选「高」后 Network `/v1/responses` body 含 `reasoning.effort=high`；④ 选「自动」不带 reasoning 字段；⑤ 自定义脚本变量列表含 reasoningEffort；⑥ 重试沿用节点快照档位；⑦ 图/视/音路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 图/视频工作台参数面板（模型能力卡 + 真实 pill）：`ImageSettingsPanel` / `VideoSettingsPanel` 改为截图风格——顶部「模型说明」卡（工作模式 / 输入规格 / 输出规格 / 返回格式 / 不支持）+ 按模型能力注册表渲染比例/秒数/清晰度/质量 pill。能力来自 `web/src/lib/model-capability.ts`，只暴露现有请求路径真实可选值（Grok 比例/1–15s/480–1080p；Seedance 含 adaptive 与 fast 禁 1080p；Sora-2 仅 1280x720/720x1280 与 4/8/12s；Veo 4/6/8s；Agnes 固定 1152x768 与 2/5s；OpenAI 生图 quality + 透明背景；Grok 生图 aspect_ratio 无透明）。切换模型时 clamp 非法残留参数；**不改** create/poll 请求路径。画布节点设置 popover 共用同一面板。需验证：① 选 `grok-imagine-video-1.5` 见比例 pill + 说明卡且秒数无 20s 假选项；② 选 `sora-2` 尺寸只有横/竖 720p，选 10s 会落到 8s；③ Seedance fast 1080p 禁用；④ 选 Grok 图模无透明背景开关、无 4k 假 pill；⑤ 选 gpt-image 仍有质量/透明背景；⑥ 切换模型后旧参数被夹到合法范围；⑦ 生成仍走原 Grok/Seedance/Sora/Veo/Agnes 路径；⑧ 画布节点参数弹层同样更新。**Docker 需 `up -d --build app` 后硬刷新。**
- 本地图片「经常消失」：根因是 GC 清理 `image_files` 时**未把生图工作台历史**算作引用（删素材/删画布会误删历史图 blob），再 hydrate 时又把失效 `blob:` 当 fallback 显示白框（元数据 width/bytes 仍在）。已修：① `cleanupUnusedImages/Media` 扫描 `image_generation_logs` / `video_generation_logs`；② GC 等待 canvas store hydrated；③ `resolveImageUrl/MediaUrl` 不再回退会话级 `blob:`；④ 画布打开时登录态先 `downloadMissingCloudBlobs` 再 hydrate。需验证：① 生成一张图 → 加入素材再删除素材 → 本机历史预览仍在；② 刷新后画布节点图仍在；③ 未登录远程 imgen 无 storageKey 的图仍可能只剩白框（预期：远程临时链，需本地落盘渠道）；④ 删画布后其它画布图不丢。**Docker 需 `up -d --build app` 后硬刷新。**
- 我的资产（原「我的素材」）+ 分类管理：导航/页面/画布选择器/工作台入口统一改称「我的资产」；资产新增可选 `category` 字段（与 kind 正交，空=未分类）。**固定主分类**始终可筛：`人物 / 场景 / 道具 / 风格参考 / 分镜 / 其他`（旧「角色」读侧映射为「人物」）；也可自定义分类。资产页与画布 `选择资产` 弹窗均展示这些分类 chips。**分类过程：** ① 卡片上「快速设分类」下拉，无需进编辑弹窗；② 顶部「智能归类未分类」按标题/标签/来源/备注/正文/提示词/文件名关键词推断（证据不足则保持未分类）；③ 新建保存时若未选手动分类会自动推断；④ 本地导入 / 画布存资产 / 图视频工作台加入 / 提示词加入资产时自动写入推断分类。旧数据无 category 仍显示「未分类」，不强制回写。云同步透传 category，签名含 category。**不改**路由 `/assets`、生成路径、GC、节点 @ 引用语义。需验证：① 顶栏/画布工具条显示「我的资产」；② 筛选条可见人物/场景/道具等；③ 新建资产选「人物」后可被人物筛出；④ 历史「角色」在筛选「人物」时命中；⑤ 卡片下拉可直接改分类并刷新筛选；⑥ 点「智能归类未分类」后有命中项变为人物/场景/道具等，无把握的仍未分类；⑦ 本地导入文件名含「肖像/风景/道具」等会自动分类；⑧ 画布选择资产同样可按人物/场景/道具筛选并插入；⑨ 刷新/云同步后分类仍在；⑩ 图/视频生成与导出/导入 zip 不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 资产提示词与可区分标题：工作台/画布加入图片或视频资产时，`metadata.prompt` 写入完整生成提示词，标题用提示词摘要（不再一律「生成结果 1 / 生成视频」）。资产详情抽屉展示「生成提示词」区块并可复制；卡片摘要/搜索可命中提示词。旧资产若已有 `metadata.prompt` 但标题仍是通用名，列表/详情**仅展示**时改用提示词摘要（不强制改写本地 title）。需验证：① 生图/视频/画布新加入资产后标题可读且互不相同；② 点开详情可见完整提示词；③ 搜索提示词可命中；④ 无 prompt 的本地导入仍只显示尺寸等；⑤ 分类/云同步/下载/去工作台不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- Grok 视频模型候选顺序：有参考图时**用户当前选择永远第一**；`grok-imagine-video-1.5` / i2v 仅作渠道列表内可选兜底，**绝不插队**。从图片模型自动切视频时优先基础 `grok-imagine-video`，不再优先 1.5（避免多参考图先撞「最多 1 张首图」）。纯文生仍只用所选模型。需验证：① 选 `grok-imagine-video` + 2 张参考图 → Network 首个 POST body 的 `model` **等于** `grok-imagine-video`，不是 1.5；② 渠道列表有 1.5 时后续候选可出现 1.5；③ 选 1.5 时首包仍是 1.5；④ 从 `*-image-quality` 带参考图自动切 → 切到基础 video 而非 1.5；⑤ 纯文生路径不变。**Docker 需 `up -d --build app` 后硬刷新。**
- Sora / Veo 图生视频完整适配（OpenAI 兼容中转，如 New API / openai2api 等 `sora-2` / `azure-sora` / `veo-3.1*`）：对齐可用脚本 `veo-sora`——**创建优先** `POST /v1/video/generations` + 最小 JSON `{model,prompt,seconds}`，再回退 `/videos`、`/videos/generations` 与 size/duration/multipart 变体。响应解包**合并嵌套** `data`/`result`/`task` 层的 `status`/`video_url`。轮询优先 `GET /v1/video/generations/{id}`；完成态含 completed/succeeded/success/ready 等；有 `video_url`/`url` 直接成功。**Sora/Veo 轮询预算约 15 分钟**（300×3s）。**Sora 模型名：** UI 仍可选 `sora-2`；创建时**先发所选**，若 422 `ModelModality.VIDEO` / `video submit failed` / Supported models 只含 `azure-sora`，**立刻**换 `azure-sora` 并**跨路径跳过**已拒收的 `sora-2`，成功后按 host 记住优先 azure（下次少打 422）。503 无渠道则换其它别名。**图生只试带参考候选**；**禁止**无参考文生回退。**不改** Grok / Seedance / Agnes。需验证：① 选 `sora-2` → Network 可先见 `model=sora-2` 的 422，**紧接着**应有 `model=azure-sora` 的 POST（不能停在 422）；② azure 创建成功后能出片或可读 503；③ 再次生成可优先 azure；④ 图生仍带参考；⑤ Grok 路径不变。**Docker 需 `up -d --build app` 后硬刷新。**
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 画布左侧停靠侧栏（复刻上游分区，适配本地）：顶栏左侧 `PanelLeft` 开关 + 可调宽停靠面板（localStorage 记忆开合/宽度），分区 **画布 / 资产 / 提示词库**。画布 Tab：搜索、类型筛选、缩略图/状态、多选导出、点击行动画聚焦（含侧栏 left inset）；图片行眼睛按钮打开「图片详情」预览。资产 Tab：本地素材库按类型折叠 + 标签筛选 + 添加/插入/移除。提示词库 Tab：按已启用来源折叠拉取，可查看详情/插入文本节点。替换旧浮动节点列表接线；`UserStatusActions` 顶栏配置按钮在「我的资产」等页右侧仍可用。需验证：① 三分区内容正确；② 开关/拖宽/记忆生效；③ 点击屏外节点平滑聚焦并单选；④ 图片可放大预览；⑤ 资产/提示词可插入画布；⑥ 浅色/深色正常；⑦ Agent、导出、生成、云同步不受影响。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- 生图参考图上限：BYOK / 自定义脚本本地硬顶由 3 提到 **6 张**（`web/src/lib/image-reference-limits.ts` + 工作台 UI + `requestEdit` 校验）；**平台积分图生图仍最多 4 张**（与 `api` `PLATFORM_IMAGE_REF_LIMIT` 对齐，未改服务端）。多图仍走既有 Grok JSON multi-image / OpenAI multipart 路径，**禁止静默只发第一张**。需验证：① 图片工作台可加到 6 张；② 第 7 张被拒绝并提示；③ 4～6 张 Grok/中转可发请求（上游不支持时错误可读）；④ 平台模式仍卡 4；⑤ 画布连线多图不因本地 3 张旧上限被截断；⑥ 文生图/单图/蒙版/脚本路径不受影响。
- 局域网 New API Grok 图生图 503：浏览器打到 `http://局域网:3000/v1/images/edits` 且响应头含 `x-new-api-version` 时，**503 来自 New API 网关/其上游渠道**，不是本站路由写错。本地已：① 保留 Grok JSON edits 成功路径；② 5xx/无渠道/鉴权失败提前停止 body 穷举；③ 错误文案提示检查 New API「渠道」绑定、分组权限、上游 Grok Key。需验证：① 503 时 UI 可读且不再长时间连刷多种 body；② 渠道修好后单图/多图 Grok 图生图仍可用；③ codex2api / 文生图 / 视频路径不受影响。
- Grok 图生图模型对齐官方（去掉自动跳 `*-edit`）：官方 xAI SDK `ImageGenerationModel` 仅 `grok-imagine-image` / `grok-imagine-image-pro` / `grok-imagine-image-quality`；多参考编辑与文生共用这些名字，靠请求是否带 `image`/`images` 区分，**没有** `grok-imagine-image-edit` 官方模型。本地已：**有参考图时不再自动把 quality/image 改成 edit**；`listGrokImageEditModelCandidates` 以用户当前选择为首，官方名列表命中次之，中转 `*-edit` 别名仅在渠道列表真实存在时加入且不注入。接口仍是 Grok JSON `POST /images/edits`。图片工作台有参考图时在参考图区/模型旁/请求模式卡展示「图生图可用模型」提示（官方名 + 渠道列表中的中转别名）。需验证：① 选 `grok-imagine-image` 或 `*-quality` + 参考图 → Network body 的 `model` **等于所选**，不是 `*-edit`；② 无「图生图已自动使用 …-edit」提示；③ 多图仍 `images:[{url},…]` 全量；④ 有参考图时 UI 提示含官方模型名；⑤ 用户若手动选渠道列表里的 `*-edit` 仍可用；⑥ 文生图/视频/非 Grok 路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布点击节点回填生成提示词：`CanvasNodePromptPanel` 对齐上游——点击已生成图片/视频等节点时，下方输入框始终显示 `metadata.prompt`，不再因已有 content 清空；同节点生成完成后仍保留当前输入便于改词重跑。顶栏主导航新增「配置」项（齿轮+文案），点击打开配置弹窗，不新增 `/config` 路由；**右侧状态区仍保留配置齿轮**（与主导航配置入口并存）。需验证：① 生成成功后点击该图节点，面板出现原提示词；② 可改词再生成；③ 顶栏左侧「配置」与右侧齿轮都能打开弹窗；④ 移动端抽屉也有配置；⑤ 画布页顶栏原有配置/Agent 不受影响。
- 工作空间进入加速 + 记住上次空间：顶栏「工作空间」若本机记过上次打开的空间且仍在成员列表中，会直接进入 `/workspace/:id`，不再每次先选。详情顶栏「切换工作空间」→ `/workspace?list=1` 才显示列表。加载：详情 `getWorkspace` 与 items/tasks 并行；元数据先出壳、列表再填；刷新为 soft 不整页空白；素材墙缩略图 IntersectionObserver 懒加载；`workspaceFileObjectUrl`/`workspaceFileText` 会话级缓存去重，卡片/详情/下载共用一次下载；非当前 Tab `destroyOnHidden` 不预拉任务交付物；列表 API `countMembers` 避免为计数扫用户。**不改** 生成/私有同步。需验证：① 进入某空间后刷新顶栏再点工作空间直达该空间；② 「切换工作空间」可换；③ 解散/失效上次 id 回列表；④ 详情首屏更快出标题/成员；⑤ 素材墙滚动才加载视口外缩略图；⑥ 点卡片再开详情不重复下同一文件；⑦ 刷新不闪白整页；⑧ 生成与其它模块无回归。**Docker 需 `up -d --build app api` 后硬刷新。**
- 服务器 HTTP 剪贴板粘贴：图/视频工作台与画布在 `http://IP:端口` 下浏览器禁止 `navigator.clipboard.read()`（非安全上下文）。已改为：① 工作台「剪切板」按钮在 HTTP 下提示改用 `Ctrl+V`；② 页面监听 `paste`，从 `clipboardData` 取截图加入参考图（提示词框内粘贴图片也生效）；③ 画布 Ctrl+V 在无内部复制节点时，HTTP 不 `preventDefault`，由 paste 事件落图/文本节点；HTTPS/localhost 仍可用按钮与 `clipboard.read`。**不改**生成请求路径。需验证：① 服务器 `http://IP:3001` 图片工作台截图后 Ctrl+V 出现参考图；② 视频工作台同①；③ 画布空白处 Ctrl+V 截图可建图片节点；④ HTTPS/localhost「剪切板」按钮仍可用；⑤ 输入框粘贴纯文本不误加参考图；⑥ 拖拽/上传/生成路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布节点提示词面板布局复刻上游：去掉顶栏「xx提示词 + 提示词库/AI 优化」文案条；输入区透明无边框；底栏为 **库图标 · AI 优化图标 · 模型 chip · 参数 pill · 发送箭头**（本地保留 AI 优化能力，仅改为图标按钮）。生成路径、@ 引用、提示词回填、resize/滚轮行为不变。需验证：① 面板外观与截图一致；② 库/优化/模型/参数/发送均可点；③ 生成与改词重跑正常；④ 图/视/音/文模式底栏控件正确。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作台参考区拖拽上传（对照上游 `edc7667`）：图片/视频工作台参考区支持把文件拖入虚线框添加参考素材，拖入高亮 + 空态文案「可将…拖到这里 / 松开即可…」。图片区 `addReferences` 只收图片；视频页图/视/音三个区共用 drop → 现有 `addReferences`（按类型分流与 Sora/Veo/Grok/Seedance 上限）。**不改**上传按钮、剪切板路径、生成请求。需验证：① 图片工作台拖入 png/jpg 出现缩略图；② 视频工作台拖图/视频/音频分别进入对应列表；③ 超限/超大文件仍有原有警告；④ 空态拖入文案与高亮正确；⑤ 上传按钮与资产选择仍可用；⑥ Grok/Sora/Veo/Seedance 限制文案仍在。**Docker 需 `up -d --build app` 后硬刷新。**
- 工作台参考图点击预览：图片/视频工作台参考图缩略图改用 antd `Image` 预览（与生成结果一致）；点击缩略图可放大查看；角标/排序/删除按钮仍在上层且可点。无 `dataUrl` 时仍显示占位，不误开预览。**不改**上传、拖拽、剪切板、生成请求。需验证：① 点参考图可预览；② 删除/排序不误触预览；③ 拖入新图后可预览；④ 生成路径不变。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布节点提示词 `@` 真缩略图 chip：节点生成面板输入框改为上游 `CanvasPromptChipInput`（contentEditable）；输入 `@` 弹出已连接素材菜单，选中后插入**真实缩略图 chip**（序列化仍是「图片1」等 label 文本，生成链路不变）。保留 AI 优化/提示词库/模型参数/发送/节点切换回填/生成后保留提示词；文本节点正文编辑仍用旧 mention textarea。需验证：① 连接参考图后 `@` 可选并插入缩略图 chip；② 发送/生成请求 prompt 含 label 文本而非 HTML；③ AI 优化流式回填后 chip 可重建；④ 库插入文本正常；⑤ 点击节点仍回填原 prompt；⑥ 图/视/音/文模式生成不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 配置界面 UI 重设计（对照上游列表+抽屉，不改本仓数据模型）：顶部 **导入配置 / 导出配置**（`config-file.ts`，含渠道/modelScripts/WebDAV/提示词来源，含密钥请自行保管）；渠道 Tab 紧凑摘要列表 + 右侧 `ChannelEditorDrawer`（名称/协议/兼容预设/接口地址/API Key + **渠道模型**行：能力 Segmented + 调用脚本 + 选择模型弹窗）；保留服务器代理/内网中继/拉取全部；**偏好设置** 只保留默认模型 + 生成偏好（**不再有「模型可选项」手选清单**）；各处下拉由渠道已保存模型按名称自动推导能力；`imageModels*` 字段仍同步写入以兼容导入导出。**调用脚本仅在渠道编辑抽屉设置**；**提示词来源** 内置 6 个 registry 源（Banana Prompt Quicker / DavidWu GPT Image 2 / Awesome GPT Image / Awesome GPT-4o / YouMind GPT Image 2 / YouMind Nano Banana Pro），优先 yukkcat registry JSON，失败回退本地 markdown 解析；自定义 JSON 源、定时拉取、WebDAV 保留。`models: string[]`、`modelScripts`、`compatProfile` 均保留。需验证：① 渠道里选中的模型直接出现在对应能力下拉；② 偏好设置无「模型可选项」区块，仅默认模型/生成偏好；③ 导入/导出不丢 modelScripts；④ 渠道抽屉可编辑/清除调用脚本；⑤ 提示词来源 6 个上游同名内置源可拉取；⑥ 自定义 JSON 源仍可用；⑦ 关闭来源开关后浏览页不再出现；⑧ 兼容预设/代理空 Key 行为不变；⑨ 我的/收藏/精选/封面生成等本地提示词功能仍可用；⑩ Grok/Sora/Veo/Seedance/Agnes 生成路径不受影响。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
- AI 接口安全代理：新增 `ai-proxy` Docker 服务和 Nginx `/ai-proxy/` 同源转发，真实上游 API Key 存放在服务器 `.env.proxy`，前端可通过“添加服务器代理”创建 Base URL 为 `/ai-proxy` 的渠道，并用代理访问令牌作为 API Key；代理未启用访问令牌时前端允许 API Key 留空。代理渠道默认包含 `agnes-video-v2.0`，Agnes Video 走 `/v1/videos` 创建任务、`/agnesapi?video_id=...` 轮询；Agnes 创建/轮询遇到 429 会做短退避重试。需验证图片生成、参考图编辑、模型列表、文本问答、音频生成、OpenAI/Seedance/Agnes 视频任务和 CORS/鉴权/限流行为。
- 远程图片落盘：图片生成结果优先本地落盘；接口若返回 base64 会直接显示。若中转只返回 `imgen.x.ai` 且本机访问不了 xAI CDN，则可能“生成成功但看不见”。媒体代理仅在**当前图片渠道**本身是 `/ai-proxy` 时启用，不会因为另有 Agnes 代理渠道而误走。需验证：本地参考图生图、纯文生图；直连中转站时不应再出现无意义 `/ai-proxy/media` 502。
- 视频参考图 / Grok 双渠道路径：文生/图生/**多参考图**都是 **generation**，**禁止**把多图 generation 打到 `POST /videos/edits`。**公网 codex2api** 固定 `POST /v1/videos/generations`（不回退 `/v1/videos`）。**内网 New API**（`192.168.x` 等私网直连）对 Grok **只走** `POST /v1/video/generations`，可兜底 `/videos`，**跳过不存在的** `/videos/generations`；路径级 `Invalid URL`/404 会负缓存，禁止多图时在错误路径上继续刷 body；`invalid api platform:48` 立即换路径。多图最多 7 张，只发 `reference_images` / `images` / `image_urls`；超过 7 张拒绝，禁止静默只发第一张。codex2api 多图更强压缩 data URI。需验证：① 画布渠道 `192.168.6.78:3000` 多图 Network **只**先打 `/v1/video/generations`，**不再**出现 `/v1/videos/generations` 404；② codex2api 仍只打 `/v1/videos/generations`；③ 多图路径不出现 `/videos/edits`；④ 两渠道纯文生/单图仍可用。**本机 Docker `3011` 需 `docker compose … up -d --build app` 后硬刷新才吃到此修复。**
- Grok **单条参考视频 edits**（中转）：上传 1 条 MP4 + 提示词时走 `POST /v1/videos/edits`（与 generation 路径/缓存隔离）；禁止同时挂参考图/音频/多条视频；约 1–15 秒；硬顶约 **100MB**（建议源文件 ≤40MB）。**codex2api 实测 multipart → 415**，前端对 codex2api/xAI **只发 JSON**（`video` / `video_url` + data URI 或公网 URL），415 会跳过 multipart 继续试 JSON 字段；New API 无 edits 时应**快速失败**并提示换中转，禁止回退 generation 静默丢视频。需验证：① codex2api + Grok + 本地短 MP4 → Network 见 `POST …/videos/edits` 且 **Content-Type: application/json**（不是 multipart），能出片或可读上游错误；② 同配置多图无视频 → 仍只打 `…/videos/generations`；③ New API + 参考视频 → 秒级失败、无 30+ 重试；④ Seedance 参考视频仍可用；⑤ 图+视频混用被前端拦截。
- Grok 视频状态查询（codex2api）：创建后轮询 **优先 `GET /v1/videos/{request_id}`**，其次 `GET /v1/videos/generations?request_id=`；**不再**优先打不存在的 `/videos/generations/{id}` / `/videos/edits/{id}`。任务级 404 只等约 3 分钟，**禁止**把整站 host 标成「无查询接口」毒化后续任务。需验证：① 参考视频 edits 创建成功后 Network 有 `GET …/v1/videos/{id}`；② 纯文生/图生 generation 同样能查到状态；③ 第一次任务 404 后，第二次新任务仍会继续查询而不是立刻报「长时间无法查询」。
- Grok 比例/时长：参数面板的 `9:16` 等比例与秒数会写入请求主候选（`aspect_ratio` + `duration`/`seconds`），**禁止**先发「仅 duration、无比例」导致中转固定 16:9；多图不再静默截断到 8 秒。需验证：① 选 9:16 + 12s 纯文生 → Network 首个 POST body 含 `aspect_ratio:"9:16"` 且 `duration:12`；② 结果竖屏；③ 选 6s 不再总是 8s；④ 9:16 失败时错误可读，不是静默回 16:9。
- Grok 所选规格真实请求（只降不升）：选中的清晰度/比例/秒数**原样写入首个请求**（文生/单图/多图/edits 一致）。顺序硬约束：① 完整用户规格（含字段变体）② 创建失败后才降档（1080→720→480；选 720 只试 720/480；选 480 不升 720）③ 去 resolution/比例的最小 body 仅末位兜底。**禁止**无分辨率 body 或更低 resolution 抢先成功。任务记录 `requestedResolution` / `acceptedResolution`；若创建降档或源文件短边明显低于所选，工作台/画布 `message.warning`，**不虚标**。需验证：① 单参考图 + 1080p → Network **首个** POST 同时含 `resolution:"1080p"`、`aspect_ratio`、用户 `duration`；② 任意 `resolution:720p` 出现在全部 1080 候选之后；③ 选 480p 时请求中**无** 720p/1080p 升档；④ 纯文生/多图/edits 首包均带用户清晰度；⑤ 若 1080 创建失败降 720 成功，有「创建降档」提示；⑥ 若请求仍是 1080 但文件 736×400，有「请求已按 1080、上游未交付」提示；⑦ 多图仍全量参考、无 `/videos/edits`；⑧ Seedance/Sora/Veo/Agnes 路径不受影响。**Docker 需 `up -d --build app` 后硬刷新。**

- Grok 参考图生视频（中转适配）：创建超时 180s；POST 响应若已含 `video.url` 直接成功。轮询优先 `GET /v1/videos/{id}`；任务创建后前约 3 分钟的 404 视为 pending。预览优先本地 blob。需验证：New API 与 codex2api 各自纯文生/单图/创建即带 URL 可播。
- 我的素材导入：支持直接选择本机图片、视频文件，也继续支持素材压缩包；新增素材表单支持视频类型，视频卡片可预览。需验证本地 png/jpg/webp、mp4/webm/mov 导入、压缩包导入，以及导入后在素材库中预览/下载。
- 默认中转站渠道：新安装默认渠道改为 `默认中转站`，Base URL 预填 `https://www.codex2api.com`，API Key 留空，预置 `gpt-image-2` / `grok-imagine-video` / `gpt-5.5` / `gpt-4o-mini-tts`。若本地仍是未改动的官方 OpenAI 默认渠道，会软迁移到该中转站；用户已自定义的渠道不被覆盖。需验证：填入中转站 API Key 后可直接选模型生图/视频/文本/音频，以及用户自建渠道仍可正常新增与使用。
- 操作台 AI 提示词优化：图片工作台与视频工作台的提示词区域新增“AI 优化”按钮，使用当前配置的文本模型把简短描述扩写成更准确、美观、可执行的中文提示词；流式回填输入框。图片优化侧重主体/构图/光线/风格，视频优化侧重动作/运镜/节奏/氛围。需验证：已配置文本模型时可优化并替换提示词；未配置文本模型时会引导打开配置；优化中禁用输入与重复点击；优化结果可直接用于开始生成。
- 提示词库最小改造：提示词中心显示来源状态与强制刷新；支持一键“用于生图 / 用于视频 / 优化后生图”；本地记录最近使用并置顶展示。需验证：强制刷新后条数/来源状态更新；点击用于生图/视频会跳转并自动填入提示词；优化后生图会自动触发 AI 优化；最近使用可复用。
- 提示词库质量整理：默认“精选（有图优先）”模式，可切换“仅有预览图 / 全部”；卡片展示用途摘要、质量标签、友好分类名；标签去噪并按频次排序。需验证：默认列表明显更干净、无预览图条目默认更少；切换“全部”可看到更多条目；详情页摘要与质量分可读。
- 提示词预览图质量：仅把可用封面算作「有图」（排除空串/占位/过短 data URI）；加载失败或尺寸过小（&lt;48px）回退「无预览图」占位，并记入 runtime 坏链黑名单；精选 / 仅有预览图**只收当前可用封面**（高分无图不再进精选前排）；列表在封面失败后本地 re-arrange。需验证：① 精选列表前排多为真实预览图，无图高分不占第一屏；② 坏图显示为「无预览图」而不是黑块，并会从精选/仅有图中移除；③ 「仅有预览图」不再混入坏链；④ 点「强制刷新」清空坏链记忆后排序仍合理；⑤ 模式文案为「精选（有效预览）」。
- 提示词单条生成预览图：卡片「生成预览 / 重生预览」与详情「生成预览图」用当前 BYOK 图片模型只生 1 张；与「用于生图」按钮分离；结果仅本地保存——我的提示词写 coverUrl，公共库写 IndexedDB 覆盖（不回写 GitHub）；未配置图片模型时引导打开配置，不静默失败、不批量灌图。需验证：① 无 Key 仍可浏览列表；② 配置模型后单条可生成并立刻出现封面；③ 公共库条目刷新后本地封面仍在（未强制刷新第三方源前）；④ 「强制刷新」第三方库不删本地封面覆盖；⑤ 生成中禁用其它条重复点；⑥ 失败文案可读且本机列表不崩。
- 提示词收纳分层：浏览 / 收藏夹 / 我的提示词三栏；最近使用改为横向快捷条并支持收藏与加入素材；我的提示词支持新建/编辑/删除；第三方条目可“存到我的提示词”。需验证：三栏切换互不混淆；收藏星标即时生效；我的提示词本地持久化；最近使用不再与主列表视觉冲突。
- 提示词卡片操作区：底部按钮改为横向可滑动操作条（支持触控滑动与鼠标滚轮横滑），避免按钮拥挤点不到。我的提示词支持 AI 优化：编辑弹窗可“AI 优化”写入草稿；卡片/详情“优化使用/优化并另存”会另存为新模板后进入生图，不覆盖原文。需验证：窄屏下按钮可横滑点到；优化后新模板出现在我的提示词。
- 提示词详情预览放大：详情保持左图右文；点击封面即可全屏放大（`createPortal`，zIndex 10000），无额外按钮。需验证：Docker 重建后卡片→详情→点封面可放大；点遮罩或关闭退出后详情仍在。
- 画布节点 AI 优化提示词：节点下方生成面板（图/视/音/文）与配置节点“组装提示词”均新增“AI 优化”按钮，复用当前文本模型，按节点模式选择图片/视频/文本/音频优化指令；流式回填输入框。配置节点优化会尽量保留 `@[node:...]` 引用，缺失时自动补回。未配置文本模型时引导打开配置。**已增强（Toonflow 产线手册精简内置）**：图片/视频润色自动识别人物/场景/道具/分镜并注入对应生产维度；风格无关、不锁死某一 art_skills 包；画布节点把已连接参考摘要写入润色上下文。工作台图/视频与提示词库共用同一入口。需验证：① 各类型节点可优化；② 优化中禁用发送/编辑；③ 配置节点引用不丢；④ 「女主定妆/渔港夜景/铁锚道具/镜头推进」分别更像人物/场景/道具/分镜；⑤ 有连接参考图时仍锚定主体；⑥ 生成与渠道路由不受影响。**Docker 需 `up -d --build app` 后硬刷新。**
- 画布节点输入交互：节点提示词面板加宽、按钮改为可换行布局并增大可点区域；文本节点正文与提示词输入滚动条加粗（`canvas-prompt-scrollbar`）。节点提示词框默认更高（约 `h-40`）、`resize-y` 可纵向拉高、框内滚轮滚动文本且不拖动画布缩放。需验证：① 长提示词可用滚轮浏览，不必只拖右侧滑条；② 右下角可拉高输入区；③ 滚轮在输入框内不缩放画布；④ 模型/参数/生成按钮不易误点。
- 画布/工作台提示词库弹窗：从旧版“仅分类标签+直接点用”升级为与提示词中心一致的选择体验——浏览/收藏夹/我的三栏、精选/全部筛选、最近使用快捷条、预览详情、收藏、使用与“优化使用”。画布节点入口按钮改为带文字的“提示词库”，并按节点模式传入 image/video/text/audio 优化上下文。需验证：画布节点、助手面板、图片/视频工作台打开库后可筛选与预览；点“使用”会填入输入框并关闭；“优化使用”依赖文本模型。
- 云端账号最小闭环：新增 `api` 服务与 Nginx `/api/` 转发；支持注册/登录/退出（HttpOnly Cookie 会话、密码 scrypt 哈希、登录注册限流、可选 `API_INVITE_CODE`）。登录后图片/视频生成成功会异步上传到服务器（用户隔离磁盘 + jobs 元数据）；`GET /api/files/:id` 需登录且校验归属，支持 Range。未登录与 API 不可用时本地功能应不受影响。需验证：注册登录、生图/生视频后提示“已同步到云端历史”、清站点数据后重登仍能通过 `/api/jobs` 与 `/api/files/:id` 访问、用户 B 无法读 A 的文件、未登录生图仍只走本地。
- 云端历史 UI（P0.5）：图片/视频工作台生成记录侧栏与移动端抽屉增加「本机 / 云端」切换；云端列表支持刷新、预览、下载、删除（仅影响服务器，不影响本机记录）。未登录时云端 Tab 禁用并提示登录。生成成功自动上云后会刷新云端列表。需验证：登录后切换云端可见记录；预览图/视频可播；下载成功；删除后列表更新且本机记录仍在；未登录仅本机。
- 本机记录云同步状态：本机历史卡片显示「同步中 / 已上云 / 上云失败 / 仅本机」；登录用户生成成功后标记 pending 再异步上云；失败可点「重试上云」且不影响本机结果。未登录标记「仅本机」。旧记录无该字段仍可正常显示。需验证：登录生成后标签变化；断 api 后失败可重试成功；未登录无强制上云。
- 云端上传幂等：同一用户 + 同一 `client_local_id` 重复上传返回已有 job（响应含 `deduped: true`），不重复占盘。文件丢失时同 id 可修复重写。需验证：同结果点两次「重试上云」云端仍一条；`data/api/uploads` 不出现重复大文件。
- 远程图/视频上云：浏览器无法 `fetch` `imgen.x.ai` / `vidgen.x.ai`（CORS）时，前端优先本地 `storageKey`/blob；否则先试同源 `/ai-proxy/media` 取字节再 multipart 上云（**即使生成走中转站**也可借 ai-proxy 落盘）；仍失败再 `POST /api/jobs/{type}/from-url`（服务端直拉，视频默认超时约 90s，`octet-stream`+.mp4 可识别）。需验证：① 中转站 Grok 视频可播放；② 本机 ai-proxy 可出网时上云成功；③ ai-proxy/api 都出不了网时失败文案可读且本机生成仍成功；④ 非法 URL 拒绝。
- 云端 P0.5 扫尾：顶栏登录后点账号可看邮箱、云端已用/上限、记录条数，支持刷新用量与退出；`GET /auth/me` 返回 `usage`+`limits`。受保护接口 401 时统一清登录态并提示一次，不连环报错。未登录生成成功提示「登录后可跨设备回看」；上云失败若空间不足提示删云端历史。备份脚本 `scripts/backup-api-data.sh` / `.ps1`；说明见 `docs/content/docs/overview/cloud-api.mdx`。需验证：登录后打开账号弹层有容量进度；手动让会话失效后点云端操作只提示一次失效；未登录生图有登录引导且本地成功；备份脚本能打出压缩包。
- 云端 API 安全与部署加固：`from-url` 对白名单域名做 DNS 解析后拒绝内网地址、限制重定向跳数、拒绝 URL 内嵌账号信息；`API_ALLOWED_ORIGINS` 不再接受 `*`；过期/吊销会话定期清理；`docker-compose.local.yml` 透传 `API_COOKIE_SECURE` / `API_INVITE_CODE` / 容量与会话相关变量。需验证：非法内网 URL 拒绝；公网 `.env` 设 Secure+邀请码后注册/登录 Cookie 正常；重建 api 后旧会话策略符合预期；断 api 未登录本地生成仍可用。
- 同源部署登录放行：默认 `API_TRUST_PROXY_SAME_ORIGIN=true`，浏览器打开 `http://公网IP:3001` 经 Nginx `/api` 登录时，Origin 与 Host/X-Forwarded-* 一致即放行，无需先手写 IP 白名单；跨站 Origin 仍拒绝。403 文案提示如何配置 `API_ALLOWED_ORIGINS`。登录弹窗透出可读错误。需验证：① `http://服务器IP:3001` 注册/登录成功；② 伪造跨站 Origin 仍 403；③ 未登录本地生图不受影响；④ HTTPS 域名场景 Cookie Secure 仍正确。
- Nginx 反代 Host 保留端口：`nginx.conf` 的 `/api/`、`/ai-proxy/` 使用 `$http_host` + `X-Forwarded-Host`（不再用会丢端口的 `$host`），避免 `http://127.0.0.1:3011` / `http://IP:3001` 同源登录被判成 `Host 视角 http://IP` 而 403。需验证：用 **127.0.0.1:3011**（非 localhost）打开站点登录/假密码应得到 `login_invalid_credentials` 而非 `origin_not_allowed`；`Origin: https://evil.example` 仍 403；服务器 `http://公网IP:3001` 登录正常。
- 本地 Vite 云 API 代理：`web/vite.config.ts` 将 `/api`、`/ai-proxy` 默认代理到 `http://127.0.0.1:3011`（配合 `docker-compose.dev-host.yml`，避开 VS Code 抢 3001/8080），避免误打到其它服务出现 `501`。需验证：本机 `bun run dev` + compose 时登录与上云不再 501。
- 视频代理失败回退：Grok/vidgen 经 `/ai-proxy/media` 落盘失败时，不再把必 502 的 media URL 写入播放地址，回退原始 `https://vidgen...` 给 `<video>` 直连。需验证：代理 502 时预览仍可能用远端 URL；控制台可有一次 media 失败但不卡死。
- 容器出网代理：`api`/`ai-proxy` 支持根目录 `.env` 的 `HTTP_PROXY`/`HTTPS_PROXY`（可用 `host.docker.internal:本地代理端口`）。浏览器开美国代理不等于容器能拉 vidgen；上云 `from-url`/media 502 时需给容器配代理或先本机落盘。需验证：配置代理并重建后 media/from-url 可成功。
- P0.5c 验稳收口：新增 [验收清单](/docs/progress/p05c-acceptance) 与 `scripts/check-cloud-stack.sh` / `.ps1`（compose 服务状态、页面、`/api/health`、`/ai-proxy/health`、`.env` 代理卫生、可选 vidgen 探测、双轨提醒）。`api` / `ai-proxy` 日志新增 `X-Request-Id` 与基础结构化前缀。需验证：重建 api/ai-proxy 后 health 返回新增字段；脚本通过；出错日志带 request id。
  - 本机自动化（重建后）：`check-cloud-stack.ps1 -BaseUrl http://127.0.0.1:3011` 硬失败 0；`/api/health` 含 `dataDir`/`envProxy`/`trustProxySameOrigin` 等字段并回 `X-Request-Id`；`/ai-proxy/health` 同步增强；停 `api` 后页面仍 200、`/api/*` 502，启回后 health 恢复；`./data/api` 用户/任务数未丢。仍待人工：登录上云、幂等重试、跨用户隔离、401 只清一次、服务器 B2。
- 云端错误 reason code：API 错误 envelope 增加 `reason`，前端 `CloudApiError` 带 `reason` 并优先用于登录/上云提示；至少覆盖 `auth_required`、`origin_not_allowed`、`storage_quota_exceeded`、`invite_code_invalid`、`email_already_registered`、`login_invalid_credentials`、`account_temporarily_locked`、`*_rate_limited`。需验证：登录/注册/容量不足/未登录场景不再只能靠中文文案匹配。
  - 本机自动化（重建后）：未登录 `/api/jobs` → `reason=auth_required`；跨站 Origin 登录 → `reason=origin_not_allowed`。其余登录/容量等场景仍待 UI 点验。
- P1.0-A 数据访问分层：`api` 新增 `users/sessions/jobs/files` repository façade，`index.js` 改走 repo 而不直接四处 `db.*`；底层仍是 JSON 文件库，接口/存储格式/前端行为不变。需验证：登录、列表、上传、from-url、删除、容量统计行为与改造前一致。
  - 本机：重建后容器内已有 `src/repositories/*` 与 `src/model/cloud-domain.js`；历史 `data/api/db.json` 仍可读写（users/jobs/files 计数保留）。完整业务路径仍待登录后点验。
- P1.0-B 领域常量收口：`JOB_TYPE` / `USER_STATUS` / 扩展 `CLOUD_ERROR_REASON`（含 remote-fetch、not_found、payload_too_large 等）；`httpError()` 统一 throw；路由 catch 透传 `error.reason`；前端 `web/src/lib/cloud-domain.ts` 镜像 reason，登录弹窗与图/视频上云空间不足提示优先 reason（`cloudErrorReason` 写入本机历史）。需验证：① 未登录/跨站 Origin 仍返回稳定 reason；② 登录失败 `login_invalid_credentials`；③ 上云 413 时 UI 走空间不足文案且本机生成成功不被改写；④ 旧本机历史无 `cloudErrorReason` 时中文回退仍可用；⑤ 重建 api 后 `data/api` 计数不丢。
- P1.0-C 积分账本与手工加额：`db.json` 新增 `credit_ledger`（append-only）与用户 `credit_balance_cents` 缓存；`GET /auth/me` 增加 `credits.balance_cents` 且 `platform_billing_enabled=false`；登录用户 `GET /api/credits/ledger`；管理员 `POST /api/admin/credits/grant`（`X-Admin-Token` / Bearer，`API_ADMIN_TOKEN` 空则 503 `admin_not_configured`），支持 `amount_cents`/`amount_yuan`、`idempotency_key` 防双记。账号弹层展示余额。需验证：① 未配置 token 时 admin 接口 503；② 配置 token 后给邮箱加额，`/auth/me` 余额变化且同幂等键不双记；③ 错误 token 401 `admin_unauthorized`；④ 用户查自己的 ledger；⑤ 未登录生图/画布仍可用，生成路径不扣积分。
- P1.0-D 平台图片网关：服务端 `POST /api/generate/image`（需登录）；仅当 `API_PLATFORM_IMAGE_ENABLED=true` 且配置了上游 Base URL/Key 时 `/auth/me.credits.platform_billing_enabled=true`；成功落盘为 `source=server_generate` job 后按 `API_PLATFORM_IMAGE_PRICE_CENTS` 扣费，`client_local_id` 幂等不双扣；失败不扣费。图片工作台在平台就绪时显示「平台积分生图」开关（默认关=BYOK）；开启后串行按张请求。需验证：① 未开环境变量时接口 503 `platform_generate_disabled`，UI 无开关；② 开启后登录用户可平台生图，余额减少；③ 同 client_local_id 重试不双扣；④ 积分不足 402；⑤ 关闭开关后仍走本地渠道；⑥ 未登录/画布 BYOK 不受影响。
- P1.0-E 平台图生图：同一网关支持 body `images: [{ data_url }]`（png/jpeg/webp，最多 4 张，单张约 12MB）时改调上游 `/images/edits`；无参考图仍走 `/images/generations`。扣费/幂等/落盘逻辑与文生图相同。图片工作台平台模式下可带参考图（本地可读 data URL）。需验证：① 无参考图仍文生图；② 1～4 张本地参考图可平台图生图并扣费；③ 非法 data URL / 超限张数 400；④ 上游 edits 不支持时错误可读且不扣费；⑤ 关闭平台开关后参考图仍走 BYOK edits。
- P1.0-F 平台视频文生：`POST /api/generate/video`（需登录）；`API_PLATFORM_VIDEO_ENABLED=true` + 上游 Base/Key（可回落图片上游配置）；仅文生视频（OpenAI 兼容创建+轮询）；成功落盘 `source=server_generate` 后按 `API_PLATFORM_VIDEO_PRICE_CENTS` 扣费，`client_local_id` 幂等。`/auth/me.credits` 增加 `platform_video_enabled` / `video_price_cents` / `video_model`。视频工作台在就绪时显示「平台积分生视频」开关（默认关）。需验证：① 未开 env 时 503；② 开启后文生视频成功并扣费；③ 带参考素材被拒绝；④ 同 id 重试不双扣；⑤ 关闭开关后仍走 Grok/Seedance/BYOK。
- P1.0-G 平台收口加固：平台图片 JSON 请求体上限提升到约 56MB（支持多张 base64 参考图，不再被 1MB `readJson` 误伤）；视频 JSON 仍小上限；`GET /api/health` 增加 `platform.image_enabled/video_enabled/model/price`（不含密钥）；前端 `web/src/lib/platform-credits.ts` 统一图/视频就绪判断与余额文案。需验证：① health 含 platform 字段且无密钥；② 平台图生图带 1～2 张本地参考图不再因 body 过大失败；③ 账号弹层在只开视频/只开图片时文案正确。
- 账号积分余额展示：登录后账号弹层**始终显示**云端积分余额（`credits.balance_cents`）；平台代生成是否就绪用文案区分，不再因 `platform_billing_enabled=false` 整块隐藏余额。工作台「平台积分生图/视频」开关仍仅在对应 `platform_*_enabled` 为 true 时出现。需验证：① 默认未开平台网关时登录可见余额（可为 ¥0.00）；② 开平台图/视频后文案显示对应能力与单价；③ BYOK 生图不扣该余额；④ 重建 app 后强刷加载新 bundle。
- 透明背景生图（BYOK）：图像设置增加「透明背景」开关；开启后文生图/图生图请求附带 `background=transparent`（仅 OpenAI 兼容路径；Gemini 不转发该字段）。依赖上游是否支持；不支持时可能忽略或报错。需验证：① 开关可开关并持久化；② 开启后 Network 请求体/form 含 background=transparent；③ 支持的中转可出透明 PNG；④ 关闭后行为与原先一致；⑤ 画布节点图像设置同样可开。
- 画布透明背景开关修复：节点 `buildNodeConfig` 此前未合并 `metadata.background`，开关写入后 UI 立刻读回空值表现为「打不开」。现已在图片节点面板、配置节点面板合并 `background`；生成元数据与重试路径会保留该字段；节点显式关闭（`""`）不会被全局配置回填。需验证：① 画布图片节点打开图像设置可稳定开启/关闭透明背景；② 配置节点图像模式同样可开；③ 开启后生成请求含 `background=transparent`；④ 关闭后再生成不再带该字段。
- 画布节点 AI 配置合并收口：抽出 `web/src/lib/canvas/canvas-node-ai-config.ts`（`mergeCanvasNodeAiConfig` / `resolveCanvasModeModel`），图片节点面板、配置节点面板与 `project` 生成路径共用同一合并规则，避免再漏字段。需验证：① 透明背景/质量/尺寸/张数在节点面板修改后仍生效；② 配置节点切换图/文/视/音时模型仍按能力回退；③ Agnes 视频生成路径仍强制尺寸与文生约束。
- 画布导出（上游 0.9 侧栏能力的安全小切片）：顶栏菜单新增「导出当前画布」（整项目 zip，含媒体）、「导出选中节点」（多选节点 zip：图/视/音文件、文本 txt、其余 json）。不引入完整左侧可调宽侧栏/插件 registry。需验证：① 导出当前画布得到 zip；② 多选节点导出；③ 未选中时「导出选中」禁用或提示；④ 未登录本地可用；⑤ 不破坏云同步。
- 模型调用脚本（上游切片，最小安全落地）：配置「模型」Tab 可为<strong>任意可选模型</strong>（不限默认模型）编辑自定义 JS 调用脚本；脚本存 `config.modelScripts[channelId::model]`，**留空完全走系统默认**（Grok/Agnes/Seedance/OpenAI/Gemini 不变）。运行时 `runModelPlugin` 仅在有脚本时接管 image/video/audio/text 入口。编辑器使用 monospace textarea（避免 Vite 下 CodeMirror 双 `@codemirror/state` 崩溃）。运行时软限制：脚本最长约 8 万字符、整次执行约 10 分钟、`sleep`/`poll` 间隔与超时钳制；超长脚本保存被拒。脚本键优先渠道限定，避免跨渠道同名模型误用；删除**该渠道**后其 `渠道::模型` 脚本会被 prune（即使其它渠道仍有同名模型）；**配置加载时也会 prune** 孤儿脚本。裸模型名解析只回落到 `resolveModelChannel` 对应渠道的限定键，不再“唯一限定脚本全局命中”。配置页列出已配置脚本可编辑/清除。全局 `ModelPicker` 显示「脚本」角标。图片/视频工作台「请求模式」在已挂脚本时显示「自定义调用脚本」。画布节点图片/视频设置按钮摘要追加「· 脚本」。在线 Agent 工具调用始终走系统默认路径（不跑文本脚本）。需验证：① 打开「编辑脚本」不再白屏；② 可给非默认模型存脚本；③ 同名模型不同渠道不会串脚本；④ 删渠道 A 后 `A::model` 脚本消失且 `B::model` 仍在；⑤ 刷新页面后孤儿脚本不会回魂；⑥ 列表可清除脚本；⑦ 画布/工作台模型下拉显示「脚本」；⑧ 工作台请求模式与画布设置摘要随脚本切换；⑨ AI 优化走文本脚本（若有）；在线 Agent 工具不走脚本；⑩ 未配置脚本路径与改造前一致；⑪ 未登录 BYOK 仍可用。
- 双轨自动冒烟：`scripts/smoke-dual-track.ps1` / `.sh` 停 `api` 后页面仍 200、health 恢复。需验证：本机 dev-host `3011` 与服务器 `3001` 各跑一次硬失败为 0；人工仍须补「api 停时未登录生图/画布」。
- 上游跟进矩阵文档：`docs/content/docs/progress/upstream-follow.mdx` 记录切片规则与优先级。需验证：新会话只读该文档不会误整仓 merge；移植切片后会更新矩阵状态列。
- P2.0-A 画布项目 JSON 云同步：登录后编辑/新建/导入会防抖 `PUT /api/projects/:id`；画布库页「同步云端」与登录自动拉取按 `updatedAt` 合并（不删仅本地项目）。未登录/云故障时本地创建编辑删除均可用。需验证：① 未登录新建画布仍本机可用；② 登录后编辑约 2s 内上云，`data/api/projects/{userId}/` 有 json；③ 另一浏览器登录「同步云端」能看到项目；④ 停 api 后本地仍可编辑；⑤ 删除本地项目会尽量删云端。
  - 本机 API 自动化：未登录 401；put/list/get/409 冲突/跨用户隔离通过。
- P2.0-A 补强（验收修复）：画布删除增加**本机 tombstone**，避免「本地已删但云删失败/离线」后下次同步把项目拉回。云端更新冲突 reason 统一 `sync_conflict`。需验证：① 登录后删画布再点同步，项目不复活；② 另一设备若仍持有并更新了同 id 且 `updatedAt` 更新，可按云端更新恢复（有意跨端编辑）。
- P2.0-B 画布媒体 blob 同步：`POST /api/blobs`（multipart `client_key`+file，幂等）；`GET /api/blobs/by-key/:key` 读流。推送前上传项目内 `image:/video:/audio:` 等 storageKey；拉取后下载本机缺失 blob 写入 IndexedDB。需验证：① 画布生成/导入图片后登录推送，云端 `uploads/.../blobs` 有文件；② 另一设备同步后节点图可显示；③ 重复推送同一 key 不双占盘（deduped）；④ 云无该媒体时不打断 JSON 同步；⑤ 未登录行为不变。
  - 本机 API 自动化：上传/deduped/下载/跨用户 404 通过。
- P2.0-C 素材库云同步：`GET/PUT /api/assets` 用户级 manifest（assets + tombstones）；登录自动拉一次 + 素材页「同步云端」；增删改防抖推送；删除写墓碑防复活；媒体走 `/api/blobs`。需验证：① 未登录素材库仍本机可用；② 登录后新增文本/图片素材可同步；③ A 删除后 B 同步不会复活；④ 旧 updatedAt 推送 409 `sync_conflict`；⑤ 用户隔离；⑥ 云故障提示且本机不丢。
  - 本机 API 自动化：put/get/409/tombstone/跨用户隔离通过。
- 素材上云标识：素材页顶部总状态 + 卡片角标/副标签展示「已上云 / 待同步 / 仅本机 / 上云失败」。需验证：① 未登录全部为「仅本机」；② 登录同步成功后为「已上云」；③ 本地新增/编辑后未推送完显示「待同步」；④ 同步失败显示「上云失败」且本机素材仍在。
- 画布上云标识：画布库顶部总状态 + 项目卡片标签展示「已上云 / 待同步 / 仅本机 / 上云失败」。需验证：① 未登录为「仅本机」；② 登录后同步/推送成功为「已上云」；③ 本地编辑后显示「待同步」；④ 推送失败显示「上云失败」。
- 画布组节点：工具栏与空白创建菜单可新建「组」；拖拽非组节点到组区域可加入并吸附；拖动组会带动子节点；删除组仅解除 `groupId` 不删子节点；组节点不可连线；小地图以灰色显示组。需验证：创建、加入/移出、复制粘贴、删除组后子节点仍在。
- 配置节点模式切换模型：切换图/文/视/音时，若当前模型不匹配能力，会自动切到对应默认模型并写回节点；生成路径 `buildGenerationConfig` 同步按能力选择模型。需验证：配置节点从生图切到视频后模型变为视频模型，再切回文本同理。
- IME 组合态 Enter：画布提示词输入与 Agent 聊天输入在中文输入法组合过程中不会误提交；仅普通 Enter 触发发送。需验证：拼音未上屏时按 Enter 只是选词，上屏后再 Enter 才提交。
- Gemini 生图比例：Gemini 生图请求会附带 `imageConfig`/`responseFormat.image`（aspectRatio、部分模型 imageSize）；匹配最近支持比例时不再被本地 3:1 校验误伤。需验证：Gemini 渠道下 16:9 / 9:16 / auto 可正常生图。
- 节点工具条显示策略：仅对当前选中节点显示顶部工具条，hover 未选中节点不再弹出；单选时自动显示，多选/清空时收起。需验证：划过其它节点不弹工具条，点击选中后工具条出现且可操作。
- OpenAI/Seedance 视频 URL 与错误兜底：轮询时若响应直接带 `video_url`/`result_url`/`url` 则直接成功；Seedance 兼容 `completed`；envelope `code` 支持字符串 `"0"`；错误信息尽量解析 msg/message。Grok/Agnes 专用路径保持不动。需验证：中转直接返回 URL 的 OpenAI 兼容视频可播；失败时提示更可读。
- 节点提示词自动聚焦：生成面板关闭 `@` 透明文字高亮层（这是已生成节点无光标的主因：有参考图连接时 Chromium 在 `color:transparent` 下 caret 消失）；聚焦时强制蓝色光标，并 blur 节点内 video/audio。需验证：带参考图/连线的已生成视频、图片节点单击后提示词框有蓝色光标。
- 空白画布双击建节点：双击空白区域弹出“选择节点”菜单，可在点击位置创建文本/图片/视频/音频/生成配置节点；单击空白或拖动画布会关闭菜单。需验证：不与节点双击、框选、平移冲突。
- 节点名称：短标签显示在节点正上方（历史过长 title/整段提示词会压缩为「视频 · 前10字」）；单击改名。新生成节点不再把整段提示词写入 title。需验证：旧视频节点名称不再铺满画面，新生成节点标题简短。
- 图片切图拖拽网格线：切分弹窗支持拖动横/竖分割线，并可增删末线、重置；切分结果按线位置裁剪。需验证：拖线后生成子节点边界正确。
- 图片工作台限并发与请求诊断：生成按钮上方展示模型、接口路径、参考图数、张数；多张时优先一次请求，不足或失败再串行补齐，避免并发 429。需验证：生成 3 张时不会同时打出 3 个独立请求（除非 bulk 失败回退串行）。
- 图生图 edits 兼容（中转）：`/v1/images/edits` 参考图先压缩（最大边约 1280 JPEG）再 multipart；超时约 180s；连接被关/404/400 时文案明确提示「渠道可能不支持图生图 / 参考图过大」。需验证：① 本地上传参考图 + gpt-image 类模型；② 用「已生成图加入参考图」再生成；③ 若中转仍 `ERR_CONNECTION_CLOSED`，UI 应出现可读说明而不是裸网络错误；④ 无参考图文生图不受影响；⑤ 参考图横滑不再刷 passive preventDefault 警告。
- codex2api 图生图旁路（不影响其它渠道）：仅 Base URL 含 `codex2api`/`chatgpt2api` 且**无蒙版**时：瘦身 edits → 缩小参考图后 generations 带 `image`（最多 2 次）→ 仍 connection closed 则**降级纯文生图**并 warning「参考图未参与生成」。官方 OpenAI、Gemini、自定义脚本、其它中转、蒙版编辑仍只走原路径。需验证：① codex2api+参考图：要么真图生图成功，要么出图并提示已降级文生；② 非 codex2api 有参考图仍只打 edits；③ 无参考图行为不变；④ 控制台不应再刷 4+ 次 generations closed。
- 内网 AI 同源中继 `/lan-ai`：浏览器不能直连 `http://192.168.x.x:端口`（CORS）。部署侧设置 `LAN_AI_UPSTREAM=192.168.6.78:8000` 后重建 app；配置里点「添加内网中继」，Base URL 填 `/lan-ai`（勿填局域网 IP），再「拉取模型」。未配置 LAN_AI_UPSTREAM 时 `/lan-ai/*` 返回 503 说明。需验证：① 拉取模型请求为同源 `/lan-ai/v1/models` 且无 CORS 红字；② 内网服务无鉴权时可留空 Key；③ 默认中转站 / 服务器代理渠道不受影响；④ 公网部署勿把内网 IP 写进可被外网滥用的开放代理。
- 内网 Grok 视频预览：生成成功但结果 URL 为 `127.0.0.1`/局域网时，浏览器会 `ERR_CONNECTION_REFUSED` 显示「远程直链」预览失败。落盘与预览会把私网视频 URL 改写为同源 `/lan-ai/...` 再下载/播放；成功后优先本地 blob。需验证：① 内网中继渠道文生视频后预览可播或标签为本地可播；② 历史「远程直链」点开时尽量走 /lan-ai；③ 公网 vidgen 路径不被误改写。
- 画布多选操作：已有 Ctrl/Cmd 框选、Shift 点选、Delete 删除。现增强：① 选中 ≥2 时底部浮条显示「已选 N 个」并提供复制/删除/取消选择；② 底栏工具条显示选中数量，删除提示含个数；③ 右键在多选集合内时「删除 N 个节点」并支持复制多选；右键未选中节点则改为单选该节点。需验证：框选多个 → 浮条删除全部；Shift 追加选择后 Delete；右键多选删除；单选右键仍只删一个。
- 公网用家里 Grok2API（FRP 方案 B）：`deploy/lan-tunnel/` 独立 frps/frpc，服务器 `LAN_AI_UPSTREAM=http://infinite-canvas-frps:18000`，前端 `/lan-ai`。需验证：① 未起 frp 时主站 3001 行为不变；② frpc 上线后 tunnel/lan-ai 为 401/200；③ 公网画布拉取模型成功。
- 渠道生图兼容预设：配置 → 渠道 →「生图兼容预设」。auto 不改行为；手动选 Grok/精简/标准 OpenAI 改变文生图字段。需验证：① 旧渠道无 compatProfile 时与改前一致；② /lan-ai 选质量/比例可出图；③ 改成「标准 OpenAI」后请求带 size/quality（若上游支持）；④ 自定义模型脚本仍优先于预设。
- 图生图兼容边界收口：工作台请求诊断复用 `describeImageRequestMode`，明确区分 Grok JSON edits、OpenAI multipart、脆弱中转旁路/降级、自定义脚本和文生图；所选模型的自定义脚本优先于 Grok 图/视频自动换模；内置图生图最多接收 3 张参考图并在超限时明确拒绝（平台/自定义脚本最多 4 张），UI 与提示词不再声称发送更多；Grok edit 候选只使用所选渠道的已拉取模型清单，有清单时不再硬猜不存在的 edit 名；Grok 视频任务保留 `channelId::model`，轮询不因同名模型串到其它 BYOK 渠道。已新增 Vitest（`web`：`npm test` / `bun run test`）覆盖请求模式文案、图/视频同渠道换模候选；**不**覆盖真实 Axios Network create/poll 契约，也不改 `web/bun.lock`（Vitest 仅写在 `package.json`，下次独立 lock 清理再并入）。需验证：① 两个渠道含同名模型时，图/视频创建与轮询始终走所选 Base URL/API Key；② 自定义脚本模型带参考图时不被自动换模；③ 第 4 张参考图被明确拒绝加入；④ 请求模式文案与 Network 的 JSON/multipart/脚本路径一致。
- Grok 图生图 JSON edits：`grok-imagine-image*` 或兼容预设 `grok-image` 时，有参考图走 `POST /v1/images/edits` + `Content-Type: application/json`，单图优先 `image: { url: dataURI }`；**多参考图只发真正的多图 payload**（`images` / `image_urls` 变体），不再混入会让中转静默退化成单图的 fallback。多图提示词会明确要求同时参考全部图片。需验证：① 本地单参考图 + grok-imagine-image-quality 可出图；② 本地双/三参考图不会再只偏第一张；③ Network 为 application/json 且无「仅支持 application/json」报错；④ OpenAI 渠道有参考图仍为 multipart edits；⑤ 蒙版编辑仍走原路径。
- 画布已有图编辑自动 Grok edit：图片节点带本地可读内容再生成时走 `requestEdit`；Grok 渠道若当前选的是 `grok-imagine-image-quality` 等文生名，即使渠道清单未列出 edit 名，也会自动优先请求 `grok-imagine-image-edit`（渠道限定 `channelId::model`）。需验证：① 画布选 Grok quality 模型编辑已有图，Network 实际 model 含 `edit`；② 清单已有真实 edit 名时用清单名；③ 工作台有参考图时同样自动切换；④ 非 Grok / 自定义脚本不自动换模。
- 文本模型渠道归属：AI 提示词优化、文本问答与工具调用解析模型时优先 `textModel`，并在优化路径保留 `channelId::model` 归属，避免多渠道存在同名模型（如多个 `grok-*`）时请求打到默认/第一个匹配渠道。需验证：配置两个都含同名文本模型的渠道，UI 选非默认渠道后点「AI 优化」，实际请求 Base URL/鉴权应走所选渠道；仅单渠道时行为与原来一致。
- 画布生成后保留提示词（上游 v0.10 切片）：节点提示词面板提交生成后**不清空**输入框；同一节点生成完成（内容写回）后仍保留刚才的提示词，便于改词再生成；切换到其它节点时再按新节点状态重置。需验证：① 新建图片/视频/文本/音频节点生成成功后输入框仍有原文；② 改几个字可直接再点生成；③ 切到另一节点再回来不串词；④ 已有内容的节点首次打开编辑时仍是空改写框（placeholder 语义不变）；⑤ 未登录 BYOK 可用。
- 画布节点名默认隐藏（上游 0.9 体验切片）：节点标题仅在**选中 / 悬停 / 正在改名**时显示；空闲时不渲染标题条，降低画面噪声。仍使用本 fork 的短标题 `displayNodeTitle`（过长提示词不会铺满）。需验证：① 空闲画布上看不到各节点名；② 悬停或选中后标题出现且可单击改名；③ 改名 Enter/Escape 行为不变；④ 组节点同样适用；⑤ 未登录本地可用。
- 提示词搜索防抖：提示词中心与画布/工作台「提示词库」弹窗的关键字输入即时回显，查询侧约 280ms 防抖后再过滤/请求。需验证：① 快速输入时列表不每键抖动；② 停顿后结果与关键字一致；③ 收藏夹/我的提示词本地过滤同样防抖；④ 画布节点打开提示词库搜索仍可用。
