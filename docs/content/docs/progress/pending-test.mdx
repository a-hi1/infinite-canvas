---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
- AI 接口安全代理：新增 `ai-proxy` Docker 服务和 Nginx `/ai-proxy/` 同源转发，真实上游 API Key 存放在服务器 `.env.proxy`，前端可通过“添加服务器代理”创建 Base URL 为 `/ai-proxy` 的渠道，并用代理访问令牌作为 API Key；代理未启用访问令牌时前端允许 API Key 留空。代理渠道默认包含 `agnes-video-v2.0`，Agnes Video 走 `/v1/videos` 创建任务、`/agnesapi?video_id=...` 轮询；Agnes 创建/轮询遇到 429 会做短退避重试。需验证图片生成、参考图编辑、模型列表、文本问答、音频生成、OpenAI/Seedance/Agnes 视频任务和 CORS/鉴权/限流行为。
- 远程图片落盘：图片生成结果优先本地落盘；接口若返回 base64 会直接显示。若中转只返回 `imgen.x.ai` 且本机访问不了 xAI CDN，则可能“生成成功但看不见”。媒体代理仅在**当前图片渠道**本身是 `/ai-proxy` 时启用，不会因为另有 Agnes 代理渠道而误走。需验证：本地参考图生图、纯文生图；直连中转站时不应再出现无意义 `/ai-proxy/media` 502。
- 视频参考图：Grok 固定走 `/v1/videos/generations`，不再回退 OpenAI `/videos`。单图 `image:{url}`，多图 `reference_images`；本地图优先压缩 data URI（最大边约 1280、约 1.5MB），远程 imgen 透传 URL；单图 I2V 会额外尝试 `grok-imagine-video-1.5`。失败时展示上游 400 原文。需验证：本地参考图、远程 imgen 参考图、无参考图文生视频。
- Grok 参考图生视频（codex2api 适配）：创建超时放宽到 180s；POST 响应若已含 `video.url` 直接成功。单图优先 `grok-imagine-video-1.5` 并兼容 `image` / `image_url` / `images` 字段；多图走 `reference_images`。轮询仅官方 `GET /v1/videos/{id}`（兼容 `/videos/generations/{id}`）；任务创建后前约 90s 的 404 视为 pending，不立刻判“中转不支持查询”。预览优先本地 blob，失败用远程 URL。需验证：本地小图参考图生视频、纯文生视频、创建响应直接带 URL 时可播。
- 我的素材导入：支持直接选择本机图片、视频文件，也继续支持素材压缩包；新增素材表单支持视频类型，视频卡片可预览。需验证本地 png/jpg/webp、mp4/webm/mov 导入、压缩包导入，以及导入后在素材库中预览/下载。
- 默认中转站渠道：新安装默认渠道改为 `默认中转站`，Base URL 预填 `https://www.codex2api.com`，API Key 留空，预置 `gpt-image-2` / `grok-imagine-video` / `gpt-5.5` / `gpt-4o-mini-tts`。若本地仍是未改动的官方 OpenAI 默认渠道，会软迁移到该中转站；用户已自定义的渠道不被覆盖。需验证：填入中转站 API Key 后可直接选模型生图/视频/文本/音频，以及用户自建渠道仍可正常新增与使用。
- 操作台 AI 提示词优化：图片工作台与视频工作台的提示词区域新增“AI 优化”按钮，使用当前配置的文本模型把简短描述扩写成更准确、美观、可执行的中文提示词；流式回填输入框。图片优化侧重主体/构图/光线/风格，视频优化侧重动作/运镜/节奏/氛围。需验证：已配置文本模型时可优化并替换提示词；未配置文本模型时会引导打开配置；优化中禁用输入与重复点击；优化结果可直接用于开始生成。
- 提示词库最小改造：提示词中心显示来源状态与强制刷新；支持一键“用于生图 / 用于视频 / 优化后生图”；本地记录最近使用并置顶展示。需验证：强制刷新后条数/来源状态更新；点击用于生图/视频会跳转并自动填入提示词；优化后生图会自动触发 AI 优化；最近使用可复用。
- 提示词库质量整理：默认“精选（有图优先）”模式，可切换“仅有预览图 / 全部”；卡片展示用途摘要、质量标签、友好分类名；标签去噪并按频次排序。需验证：默认列表明显更干净、无预览图条目默认更少；切换“全部”可看到更多条目；详情页摘要与质量分可读。
- 提示词预览图质量：仅把可用封面算作「有图」（排除空串/占位/过短 data URI）；加载失败或尺寸过小（&lt;48px）回退「无预览图」占位，并记入 runtime 坏链黑名单；精选 / 仅有预览图**只收当前可用封面**（高分无图不再进精选前排）；列表在封面失败后本地 re-arrange。需验证：① 精选列表前排多为真实预览图，无图高分不占第一屏；② 坏图显示为「无预览图」而不是黑块，并会从精选/仅有图中移除；③ 「仅有预览图」不再混入坏链；④ 点「强制刷新」清空坏链记忆后排序仍合理；⑤ 模式文案为「精选（有效预览）」。
- 提示词单条生成预览图：卡片「生成预览 / 重生预览」与详情「生成预览图」用当前 BYOK 图片模型只生 1 张；与「用于生图」按钮分离；结果仅本地保存——我的提示词写 coverUrl，公共库写 IndexedDB 覆盖（不回写 GitHub）；未配置图片模型时引导打开配置，不静默失败、不批量灌图。需验证：① 无 Key 仍可浏览列表；② 配置模型后单条可生成并立刻出现封面；③ 公共库条目刷新后本地封面仍在（未强制刷新第三方源前）；④ 「强制刷新」第三方库不删本地封面覆盖；⑤ 生成中禁用其它条重复点；⑥ 失败文案可读且本机列表不崩。
- 提示词收纳分层：浏览 / 收藏夹 / 我的提示词三栏；最近使用改为横向快捷条并支持收藏与加入素材；我的提示词支持新建/编辑/删除；第三方条目可“存到我的提示词”。需验证：三栏切换互不混淆；收藏星标即时生效；我的提示词本地持久化；最近使用不再与主列表视觉冲突。
- 提示词卡片操作区：底部按钮改为横向可滑动操作条（支持触控滑动与鼠标滚轮横滑），避免按钮拥挤点不到。我的提示词支持 AI 优化：编辑弹窗可“AI 优化”写入草稿；卡片/详情“优化使用/优化并另存”会另存为新模板后进入生图，不覆盖原文。需验证：窄屏下按钮可横滑点到；优化后新模板出现在我的提示词。
- 提示词详情预览放大：详情保持左图右文；点击封面即可全屏放大（`createPortal`，zIndex 10000），无额外按钮。需验证：Docker 重建后卡片→详情→点封面可放大；点遮罩或关闭退出后详情仍在。
- 画布节点 AI 优化提示词：节点下方生成面板（图/视/音/文）与配置节点“组装提示词”均新增“AI 优化”按钮，复用当前文本模型，按节点模式选择图片/视频/文本/音频优化指令；流式回填输入框。配置节点优化会尽量保留 `@[node:...]` 引用，缺失时自动补回。未配置文本模型时引导打开配置。需验证：各类型节点可优化；优化中禁用发送/编辑；配置节点引用素材不丢。
- 画布节点输入交互：节点提示词面板加宽、按钮改为可换行布局并增大可点区域；文本节点正文与提示词输入滚动条加粗（`canvas-prompt-scrollbar`），降低右侧细滚动条难拖的问题。需验证：长提示词滚动条可轻松拖动；模型/参数/生成按钮不易误点。
- 画布/工作台提示词库弹窗：从旧版“仅分类标签+直接点用”升级为与提示词中心一致的选择体验——浏览/收藏夹/我的三栏、精选/全部筛选、最近使用快捷条、预览详情、收藏、使用与“优化使用”。画布节点入口按钮改为带文字的“提示词库”，并按节点模式传入 image/video/text/audio 优化上下文。需验证：画布节点、助手面板、图片/视频工作台打开库后可筛选与预览；点“使用”会填入输入框并关闭；“优化使用”依赖文本模型。
- 云端账号最小闭环：新增 `api` 服务与 Nginx `/api/` 转发；支持注册/登录/退出（HttpOnly Cookie 会话、密码 scrypt 哈希、登录注册限流、可选 `API_INVITE_CODE`）。登录后图片/视频生成成功会异步上传到服务器（用户隔离磁盘 + jobs 元数据）；`GET /api/files/:id` 需登录且校验归属，支持 Range。未登录与 API 不可用时本地功能应不受影响。需验证：注册登录、生图/生视频后提示“已同步到云端历史”、清站点数据后重登仍能通过 `/api/jobs` 与 `/api/files/:id` 访问、用户 B 无法读 A 的文件、未登录生图仍只走本地。
- 云端历史 UI（P0.5）：图片/视频工作台生成记录侧栏与移动端抽屉增加「本机 / 云端」切换；云端列表支持刷新、预览、下载、删除（仅影响服务器，不影响本机记录）。未登录时云端 Tab 禁用并提示登录。生成成功自动上云后会刷新云端列表。需验证：登录后切换云端可见记录；预览图/视频可播；下载成功；删除后列表更新且本机记录仍在；未登录仅本机。
- 本机记录云同步状态：本机历史卡片显示「同步中 / 已上云 / 上云失败 / 仅本机」；登录用户生成成功后标记 pending 再异步上云；失败可点「重试上云」且不影响本机结果。未登录标记「仅本机」。旧记录无该字段仍可正常显示。需验证：登录生成后标签变化；断 api 后失败可重试成功；未登录无强制上云。
- 云端上传幂等：同一用户 + 同一 `client_local_id` 重复上传返回已有 job（响应含 `deduped: true`），不重复占盘。文件丢失时同 id 可修复重写。需验证：同结果点两次「重试上云」云端仍一条；`data/api/uploads` 不出现重复大文件。
- 远程图/视频上云：浏览器无法 `fetch` `imgen.x.ai` / `vidgen.x.ai`（CORS）时，前端优先本地 `storageKey`/blob；否则先试同源 `/ai-proxy/media` 取字节再 multipart 上云（**即使生成走中转站**也可借 ai-proxy 落盘）；仍失败再 `POST /api/jobs/{type}/from-url`（服务端直拉，视频默认超时约 90s，`octet-stream`+.mp4 可识别）。需验证：① 中转站 Grok 视频可播放；② 本机 ai-proxy 可出网时上云成功；③ ai-proxy/api 都出不了网时失败文案可读且本机生成仍成功；④ 非法 URL 拒绝。
- 云端 P0.5 扫尾：顶栏登录后点账号可看邮箱、云端已用/上限、记录条数，支持刷新用量与退出；`GET /auth/me` 返回 `usage`+`limits`。受保护接口 401 时统一清登录态并提示一次，不连环报错。未登录生成成功提示「登录后可跨设备回看」；上云失败若空间不足提示删云端历史。备份脚本 `scripts/backup-api-data.sh` / `.ps1`；说明见 `docs/content/docs/overview/cloud-api.mdx`。需验证：登录后打开账号弹层有容量进度；手动让会话失效后点云端操作只提示一次失效；未登录生图有登录引导且本地成功；备份脚本能打出压缩包。
- 云端 API 安全与部署加固：`from-url` 对白名单域名做 DNS 解析后拒绝内网地址、限制重定向跳数、拒绝 URL 内嵌账号信息；`API_ALLOWED_ORIGINS` 不再接受 `*`；过期/吊销会话定期清理；`docker-compose.local.yml` 透传 `API_COOKIE_SECURE` / `API_INVITE_CODE` / 容量与会话相关变量。需验证：非法内网 URL 拒绝；公网 `.env` 设 Secure+邀请码后注册/登录 Cookie 正常；重建 api 后旧会话策略符合预期；断 api 未登录本地生成仍可用。
- 同源部署登录放行：默认 `API_TRUST_PROXY_SAME_ORIGIN=true`，浏览器打开 `http://公网IP:3001` 经 Nginx `/api` 登录时，Origin 与 Host/X-Forwarded-* 一致即放行，无需先手写 IP 白名单；跨站 Origin 仍拒绝。403 文案提示如何配置 `API_ALLOWED_ORIGINS`。登录弹窗透出可读错误。需验证：① `http://服务器IP:3001` 注册/登录成功；② 伪造跨站 Origin 仍 403；③ 未登录本地生图不受影响；④ HTTPS 域名场景 Cookie Secure 仍正确。
- Nginx 反代 Host 保留端口：`nginx.conf` 的 `/api/`、`/ai-proxy/` 使用 `$http_host` + `X-Forwarded-Host`（不再用会丢端口的 `$host`），避免 `http://127.0.0.1:3011` / `http://IP:3001` 同源登录被判成 `Host 视角 http://IP` 而 403。需验证：用 **127.0.0.1:3011**（非 localhost）打开站点登录/假密码应得到 `login_invalid_credentials` 而非 `origin_not_allowed`；`Origin: https://evil.example` 仍 403；服务器 `http://公网IP:3001` 登录正常。
- 本地 Vite 云 API 代理：`web/vite.config.ts` 将 `/api`、`/ai-proxy` 默认代理到 `http://127.0.0.1:3011`（配合 `docker-compose.dev-host.yml`，避开 VS Code 抢 3001/8080），避免误打到其它服务出现 `501`。需验证：本机 `bun run dev` + compose 时登录与上云不再 501。
- 视频代理失败回退：Grok/vidgen 经 `/ai-proxy/media` 落盘失败时，不再把必 502 的 media URL 写入播放地址，回退原始 `https://vidgen...` 给 `<video>` 直连。需验证：代理 502 时预览仍可能用远端 URL；控制台可有一次 media 失败但不卡死。
- 容器出网代理：`api`/`ai-proxy` 支持根目录 `.env` 的 `HTTP_PROXY`/`HTTPS_PROXY`（可用 `host.docker.internal:本地代理端口`）。浏览器开美国代理不等于容器能拉 vidgen；上云 `from-url`/media 502 时需给容器配代理或先本机落盘。需验证：配置代理并重建后 media/from-url 可成功。
- P0.5c 验稳收口：新增 [验收清单](/docs/progress/p05c-acceptance) 与 `scripts/check-cloud-stack.sh` / `.ps1`（compose 服务状态、页面、`/api/health`、`/ai-proxy/health`、`.env` 代理卫生、可选 vidgen 探测、双轨提醒）。`api` / `ai-proxy` 日志新增 `X-Request-Id` 与基础结构化前缀。需验证：重建 api/ai-proxy 后 health 返回新增字段；脚本通过；出错日志带 request id。
  - 本机自动化（重建后）：`check-cloud-stack.ps1 -BaseUrl http://127.0.0.1:3011` 硬失败 0；`/api/health` 含 `dataDir`/`envProxy`/`trustProxySameOrigin` 等字段并回 `X-Request-Id`；`/ai-proxy/health` 同步增强；停 `api` 后页面仍 200、`/api/*` 502，启回后 health 恢复；`./data/api` 用户/任务数未丢。仍待人工：登录上云、幂等重试、跨用户隔离、401 只清一次、服务器 B2。
- 云端错误 reason code：API 错误 envelope 增加 `reason`，前端 `CloudApiError` 带 `reason` 并优先用于登录/上云提示；至少覆盖 `auth_required`、`origin_not_allowed`、`storage_quota_exceeded`、`invite_code_invalid`、`email_already_registered`、`login_invalid_credentials`、`account_temporarily_locked`、`*_rate_limited`。需验证：登录/注册/容量不足/未登录场景不再只能靠中文文案匹配。
  - 本机自动化（重建后）：未登录 `/api/jobs` → `reason=auth_required`；跨站 Origin 登录 → `reason=origin_not_allowed`。其余登录/容量等场景仍待 UI 点验。
- P1.0-A 数据访问分层：`api` 新增 `users/sessions/jobs/files` repository façade，`index.js` 改走 repo 而不直接四处 `db.*`；底层仍是 JSON 文件库，接口/存储格式/前端行为不变。需验证：登录、列表、上传、from-url、删除、容量统计行为与改造前一致。
  - 本机：重建后容器内已有 `src/repositories/*` 与 `src/model/cloud-domain.js`；历史 `data/api/db.json` 仍可读写（users/jobs/files 计数保留）。完整业务路径仍待登录后点验。
- P1.0-B 领域常量收口：`JOB_TYPE` / `USER_STATUS` / 扩展 `CLOUD_ERROR_REASON`（含 remote-fetch、not_found、payload_too_large 等）；`httpError()` 统一 throw；路由 catch 透传 `error.reason`；前端 `web/src/lib/cloud-domain.ts` 镜像 reason，登录弹窗与图/视频上云空间不足提示优先 reason（`cloudErrorReason` 写入本机历史）。需验证：① 未登录/跨站 Origin 仍返回稳定 reason；② 登录失败 `login_invalid_credentials`；③ 上云 413 时 UI 走空间不足文案且本机生成成功不被改写；④ 旧本机历史无 `cloudErrorReason` 时中文回退仍可用；⑤ 重建 api 后 `data/api` 计数不丢。
- P1.0-C 积分账本与手工加额：`db.json` 新增 `credit_ledger`（append-only）与用户 `credit_balance_cents` 缓存；`GET /auth/me` 增加 `credits.balance_cents` 且 `platform_billing_enabled=false`；登录用户 `GET /api/credits/ledger`；管理员 `POST /api/admin/credits/grant`（`X-Admin-Token` / Bearer，`API_ADMIN_TOKEN` 空则 503 `admin_not_configured`），支持 `amount_cents`/`amount_yuan`、`idempotency_key` 防双记。账号弹层展示余额。需验证：① 未配置 token 时 admin 接口 503；② 配置 token 后给邮箱加额，`/auth/me` 余额变化且同幂等键不双记；③ 错误 token 401 `admin_unauthorized`；④ 用户查自己的 ledger；⑤ 未登录生图/画布仍可用，生成路径不扣积分。
- P1.0-D 平台图片网关：服务端 `POST /api/generate/image`（需登录）；仅当 `API_PLATFORM_IMAGE_ENABLED=true` 且配置了上游 Base URL/Key 时 `/auth/me.credits.platform_billing_enabled=true`；成功落盘为 `source=server_generate` job 后按 `API_PLATFORM_IMAGE_PRICE_CENTS` 扣费，`client_local_id` 幂等不双扣；失败不扣费。图片工作台在平台就绪时显示「平台积分生图」开关（默认关=BYOK）；开启后串行按张请求。需验证：① 未开环境变量时接口 503 `platform_generate_disabled`，UI 无开关；② 开启后登录用户可平台生图，余额减少；③ 同 client_local_id 重试不双扣；④ 积分不足 402；⑤ 关闭开关后仍走本地渠道；⑥ 未登录/画布 BYOK 不受影响。
- P1.0-E 平台图生图：同一网关支持 body `images: [{ data_url }]`（png/jpeg/webp，最多 4 张，单张约 12MB）时改调上游 `/images/edits`；无参考图仍走 `/images/generations`。扣费/幂等/落盘逻辑与文生图相同。图片工作台平台模式下可带参考图（本地可读 data URL）。需验证：① 无参考图仍文生图；② 1～4 张本地参考图可平台图生图并扣费；③ 非法 data URL / 超限张数 400；④ 上游 edits 不支持时错误可读且不扣费；⑤ 关闭平台开关后参考图仍走 BYOK edits。
- P1.0-F 平台视频文生：`POST /api/generate/video`（需登录）；`API_PLATFORM_VIDEO_ENABLED=true` + 上游 Base/Key（可回落图片上游配置）；仅文生视频（OpenAI 兼容创建+轮询）；成功落盘 `source=server_generate` 后按 `API_PLATFORM_VIDEO_PRICE_CENTS` 扣费，`client_local_id` 幂等。`/auth/me.credits` 增加 `platform_video_enabled` / `video_price_cents` / `video_model`。视频工作台在就绪时显示「平台积分生视频」开关（默认关）。需验证：① 未开 env 时 503；② 开启后文生视频成功并扣费；③ 带参考素材被拒绝；④ 同 id 重试不双扣；⑤ 关闭开关后仍走 Grok/Seedance/BYOK。
- P1.0-G 平台收口加固：平台图片 JSON 请求体上限提升到约 56MB（支持多张 base64 参考图，不再被 1MB `readJson` 误伤）；视频 JSON 仍小上限；`GET /api/health` 增加 `platform.image_enabled/video_enabled/model/price`（不含密钥）；前端 `web/src/lib/platform-credits.ts` 统一图/视频就绪判断与余额文案。需验证：① health 含 platform 字段且无密钥；② 平台图生图带 1～2 张本地参考图不再因 body 过大失败；③ 账号弹层在只开视频/只开图片时文案正确。
- 账号积分余额展示：登录后账号弹层**始终显示**云端积分余额（`credits.balance_cents`）；平台代生成是否就绪用文案区分，不再因 `platform_billing_enabled=false` 整块隐藏余额。工作台「平台积分生图/视频」开关仍仅在对应 `platform_*_enabled` 为 true 时出现。需验证：① 默认未开平台网关时登录可见余额（可为 ¥0.00）；② 开平台图/视频后文案显示对应能力与单价；③ BYOK 生图不扣该余额；④ 重建 app 后强刷加载新 bundle。
- 透明背景生图（BYOK）：图像设置增加「透明背景」开关；开启后文生图/图生图请求附带 `background=transparent`（仅 OpenAI 兼容路径；Gemini 不转发该字段）。依赖上游是否支持；不支持时可能忽略或报错。需验证：① 开关可开关并持久化；② 开启后 Network 请求体/form 含 background=transparent；③ 支持的中转可出透明 PNG；④ 关闭后行为与原先一致；⑤ 画布节点图像设置同样可开。
- 画布透明背景开关修复：节点 `buildNodeConfig` 此前未合并 `metadata.background`，开关写入后 UI 立刻读回空值表现为「打不开」。现已在图片节点面板、配置节点面板合并 `background`；生成元数据与重试路径会保留该字段；节点显式关闭（`""`）不会被全局配置回填。需验证：① 画布图片节点打开图像设置可稳定开启/关闭透明背景；② 配置节点图像模式同样可开；③ 开启后生成请求含 `background=transparent`；④ 关闭后再生成不再带该字段。
- 画布节点 AI 配置合并收口：抽出 `web/src/lib/canvas/canvas-node-ai-config.ts`（`mergeCanvasNodeAiConfig` / `resolveCanvasModeModel`），图片节点面板、配置节点面板与 `project` 生成路径共用同一合并规则，避免再漏字段。需验证：① 透明背景/质量/尺寸/张数在节点面板修改后仍生效；② 配置节点切换图/文/视/音时模型仍按能力回退；③ Agnes 视频生成路径仍强制尺寸与文生约束。
- 画布导出（上游 0.9 侧栏能力的安全小切片）：顶栏菜单新增「导出当前画布」（整项目 zip，含媒体）、「导出选中节点」（多选节点 zip：图/视/音文件、文本 txt、其余 json）。不引入完整左侧可调宽侧栏/插件 registry。需验证：① 导出当前画布得到 zip；② 多选节点导出；③ 未选中时「导出选中」禁用或提示；④ 未登录本地可用；⑤ 不破坏云同步。
- 模型调用脚本（上游切片，最小安全落地）：配置「模型」Tab 可为<strong>任意可选模型</strong>（不限默认模型）编辑自定义 JS 调用脚本；脚本存 `config.modelScripts[channelId::model]`，**留空完全走系统默认**（Grok/Agnes/Seedance/OpenAI/Gemini 不变）。运行时 `runModelPlugin` 仅在有脚本时接管 image/video/audio/text 入口。编辑器使用 monospace textarea（避免 Vite 下 CodeMirror 双 `@codemirror/state` 崩溃）。运行时软限制：脚本最长约 8 万字符、整次执行约 10 分钟、`sleep`/`poll` 间隔与超时钳制；超长脚本保存被拒。脚本键优先渠道限定，避免跨渠道同名模型误用；删除**该渠道**后其 `渠道::模型` 脚本会被 prune（即使其它渠道仍有同名模型）；**配置加载时也会 prune** 孤儿脚本。裸模型名解析只回落到 `resolveModelChannel` 对应渠道的限定键，不再“唯一限定脚本全局命中”。配置页列出已配置脚本可编辑/清除。全局 `ModelPicker` 显示「脚本」角标。图片/视频工作台「请求模式」在已挂脚本时显示「自定义调用脚本」。画布节点图片/视频设置按钮摘要追加「· 脚本」。在线 Agent 工具调用始终走系统默认路径（不跑文本脚本）。需验证：① 打开「编辑脚本」不再白屏；② 可给非默认模型存脚本；③ 同名模型不同渠道不会串脚本；④ 删渠道 A 后 `A::model` 脚本消失且 `B::model` 仍在；⑤ 刷新页面后孤儿脚本不会回魂；⑥ 列表可清除脚本；⑦ 画布/工作台模型下拉显示「脚本」；⑧ 工作台请求模式与画布设置摘要随脚本切换；⑨ AI 优化走文本脚本（若有）；在线 Agent 工具不走脚本；⑩ 未配置脚本路径与改造前一致；⑪ 未登录 BYOK 仍可用。
- 双轨自动冒烟：`scripts/smoke-dual-track.ps1` / `.sh` 停 `api` 后页面仍 200、health 恢复。需验证：本机 dev-host `3011` 与服务器 `3001` 各跑一次硬失败为 0；人工仍须补「api 停时未登录生图/画布」。
- 上游跟进矩阵文档：`docs/content/docs/progress/upstream-follow.mdx` 记录切片规则与优先级。需验证：新会话只读该文档不会误整仓 merge；移植切片后会更新矩阵状态列。
- P2.0-A 画布项目 JSON 云同步：登录后编辑/新建/导入会防抖 `PUT /api/projects/:id`；画布库页「同步云端」与登录自动拉取按 `updatedAt` 合并（不删仅本地项目）。未登录/云故障时本地创建编辑删除均可用。需验证：① 未登录新建画布仍本机可用；② 登录后编辑约 2s 内上云，`data/api/projects/{userId}/` 有 json；③ 另一浏览器登录「同步云端」能看到项目；④ 停 api 后本地仍可编辑；⑤ 删除本地项目会尽量删云端。
  - 本机 API 自动化：未登录 401；put/list/get/409 冲突/跨用户隔离通过。
- P2.0-A 补强（验收修复）：画布删除增加**本机 tombstone**，避免「本地已删但云删失败/离线」后下次同步把项目拉回。云端更新冲突 reason 统一 `sync_conflict`。需验证：① 登录后删画布再点同步，项目不复活；② 另一设备若仍持有并更新了同 id 且 `updatedAt` 更新，可按云端更新恢复（有意跨端编辑）。
- P2.0-B 画布媒体 blob 同步：`POST /api/blobs`（multipart `client_key`+file，幂等）；`GET /api/blobs/by-key/:key` 读流。推送前上传项目内 `image:/video:/audio:` 等 storageKey；拉取后下载本机缺失 blob 写入 IndexedDB。需验证：① 画布生成/导入图片后登录推送，云端 `uploads/.../blobs` 有文件；② 另一设备同步后节点图可显示；③ 重复推送同一 key 不双占盘（deduped）；④ 云无该媒体时不打断 JSON 同步；⑤ 未登录行为不变。
  - 本机 API 自动化：上传/deduped/下载/跨用户 404 通过。
- P2.0-C 素材库云同步：`GET/PUT /api/assets` 用户级 manifest（assets + tombstones）；登录自动拉一次 + 素材页「同步云端」；增删改防抖推送；删除写墓碑防复活；媒体走 `/api/blobs`。需验证：① 未登录素材库仍本机可用；② 登录后新增文本/图片素材可同步；③ A 删除后 B 同步不会复活；④ 旧 updatedAt 推送 409 `sync_conflict`；⑤ 用户隔离；⑥ 云故障提示且本机不丢。
  - 本机 API 自动化：put/get/409/tombstone/跨用户隔离通过。
- 素材上云标识：素材页顶部总状态 + 卡片角标/副标签展示「已上云 / 待同步 / 仅本机 / 上云失败」。需验证：① 未登录全部为「仅本机」；② 登录同步成功后为「已上云」；③ 本地新增/编辑后未推送完显示「待同步」；④ 同步失败显示「上云失败」且本机素材仍在。
- 画布上云标识：画布库顶部总状态 + 项目卡片标签展示「已上云 / 待同步 / 仅本机 / 上云失败」。需验证：① 未登录为「仅本机」；② 登录后同步/推送成功为「已上云」；③ 本地编辑后显示「待同步」；④ 推送失败显示「上云失败」。
- 画布组节点：工具栏与空白创建菜单可新建「组」；拖拽非组节点到组区域可加入并吸附；拖动组会带动子节点；删除组仅解除 `groupId` 不删子节点；组节点不可连线；小地图以灰色显示组。需验证：创建、加入/移出、复制粘贴、删除组后子节点仍在。
- 配置节点模式切换模型：切换图/文/视/音时，若当前模型不匹配能力，会自动切到对应默认模型并写回节点；生成路径 `buildGenerationConfig` 同步按能力选择模型。需验证：配置节点从生图切到视频后模型变为视频模型，再切回文本同理。
- IME 组合态 Enter：画布提示词输入与 Agent 聊天输入在中文输入法组合过程中不会误提交；仅普通 Enter 触发发送。需验证：拼音未上屏时按 Enter 只是选词，上屏后再 Enter 才提交。
- Gemini 生图比例：Gemini 生图请求会附带 `imageConfig`/`responseFormat.image`（aspectRatio、部分模型 imageSize）；匹配最近支持比例时不再被本地 3:1 校验误伤。需验证：Gemini 渠道下 16:9 / 9:16 / auto 可正常生图。
- 节点工具条显示策略：仅对当前选中节点显示顶部工具条，hover 未选中节点不再弹出；单选时自动显示，多选/清空时收起。需验证：划过其它节点不弹工具条，点击选中后工具条出现且可操作。
- OpenAI/Seedance 视频 URL 与错误兜底：轮询时若响应直接带 `video_url`/`result_url`/`url` 则直接成功；Seedance 兼容 `completed`；envelope `code` 支持字符串 `"0"`；错误信息尽量解析 msg/message。Grok/Agnes 专用路径保持不动。需验证：中转直接返回 URL 的 OpenAI 兼容视频可播；失败时提示更可读。
- 节点提示词自动聚焦：生成面板关闭 `@` 透明文字高亮层（这是已生成节点无光标的主因：有参考图连接时 Chromium 在 `color:transparent` 下 caret 消失）；聚焦时强制蓝色光标，并 blur 节点内 video/audio。需验证：带参考图/连线的已生成视频、图片节点单击后提示词框有蓝色光标。
- 空白画布双击建节点：双击空白区域弹出“选择节点”菜单，可在点击位置创建文本/图片/视频/音频/生成配置节点；单击空白或拖动画布会关闭菜单。需验证：不与节点双击、框选、平移冲突。
- 节点名称：短标签显示在节点正上方（历史过长 title/整段提示词会压缩为「视频 · 前10字」）；单击改名。新生成节点不再把整段提示词写入 title。需验证：旧视频节点名称不再铺满画面，新生成节点标题简短。
- 图片切图拖拽网格线：切分弹窗支持拖动横/竖分割线，并可增删末线、重置；切分结果按线位置裁剪。需验证：拖线后生成子节点边界正确。
- 图片工作台限并发与请求诊断：生成按钮上方展示模型、接口路径、参考图数、张数；多张时优先一次请求，不足或失败再串行补齐，避免并发 429。需验证：生成 3 张时不会同时打出 3 个独立请求（除非 bulk 失败回退串行）。
- 文本模型渠道归属：AI 提示词优化、文本问答与工具调用解析模型时优先 `textModel`，并在优化路径保留 `channelId::model` 归属，避免多渠道存在同名模型（如多个 `grok-*`）时请求打到默认/第一个匹配渠道。需验证：配置两个都含同名文本模型的渠道，UI 选非默认渠道后点「AI 优化」，实际请求 Base URL/鉴权应走所选渠道；仅单渠道时行为与原来一致。
