---
title: 上游跟进矩阵
description: basketikun 上游能力只读跟踪与切片移植优先级（禁止整仓 merge）
---

# 上游跟进矩阵

本文档给后续开发/新会话用：**只读跟踪上游，按切片移植**。  
**不是**版本号对齐清单；本 fork 主线仍是本地 BYOK + 可选云历史 + `ai-proxy`。

## 对照基线（写文档时）

| 项 | 值 |
| --- | --- |
| 自用 remote | `a-hi1` → `https://github.com/a-hi1/infinite-canvas.git` |
| 上游 remote | `origin` → `basketikun/infinite-canvas`（只 fetch，默认不 push） |
| 分叉点 | 约 `568f0f1`（2026-07-05） |
| 本 fork tip（参考） | `main` / `a-hi1/main`（含 Seedance 3+ role、Grok 相对 video.url 等自用修复） |
| 上游 tip（参考） | `origin/main` 已至 **`v0.15.1`**（2026-08；含 MIT 协议更换）；矩阵仍按切片跟踪，**勿整包对齐版本号** |
| 禁止 | 整仓 `merge`/`rebase` 上游；用上游 `docker-compose`/`nginx`/`Dockerfile` 覆盖本仓库端口与 `/api` `/ai-proxy` |

核对命令：

```bash
git fetch origin --tags --prune
git merge-base HEAD origin/main
git rev-list --left-right --count HEAD...origin/main
git log --oneline HEAD..origin/main | head
```

## 硬规则

1. 推送：`git push a-hi1 main`（不要默认 `origin`）。
2. 部署心脏锁定：`3001:3000`、dev-host `3011`、`nginx` 的 `/api/` `/ai-proxy/`、`$http_host` 保留端口。
3. 一次一事：一个移植分支只带一个能力切片；合入前跑 `scripts/check-cloud-stack.*` + 双轨冒烟。
4. 安全：远程插件/自定义脚本默认按「不可信代码」评估；平台扣积分与支付产品面后置。
5. 文档：切片完成后更新本表「状态」列，并写 `pending-test.mdx`；勿只改代码不改矩阵。

## 能力矩阵

| 上游能力（约版本） | 对本 fork 价值 | 冲突/风险面 | 状态 | 建议时机 |
| --- | --- | --- | --- | --- |
| 透明背景生图（0.8.2） | 中 | 低：`image` 设置 + OpenAI 兼容请求字段 | **已移植**（BYOK） | 已完成 |
| 组节点（0.7.x） | 中高 | 中：选择/拖拽/工具条/节点类型 | **已移植（最小切片）** | 已完成；后续可增强组标题编辑/批量操作 |
| 模型自定义 JS 调用脚本（0.8.x） | 高 | 中高：与 `services/api/*`、中转特判交叉 | **已移植（最小切片）** | 已完成：旁路 `modelScripts` 映射，空脚本=默认路径；未迁 ChannelModel 对象结构 |
| 画布插件系统 + SDK + registry（0.8/0.9） | 很高 | **很高**：节点注册/打包/远程脚本安全 | 观察 | 独立工程；先白名单策略 |
| Agent 全站侧栏 / streamdown / site tools（0.7） | 中（视使用频率） | 中高：布局与 Agent 状态 | 低优先 | 仅高频 Codex 全站时 |
| 0.9 画布面板调宽/资产卡/多选导出 | 中 | 高：完整侧栏依赖 `node-registry`/插件与 `project.tsx` 大改 | **部分切片** | **已移植**：导出当前画布 + 导出选中节点；**节点名默认隐藏**（选中/悬停/改名时显示，仍用本 fork 短标题 `displayNodeTitle`）；**无资产最小节点列表**（浮动开关、搜索/类型筛选、状态、点击居中）。**仍明确后置**：完整可调宽侧栏、侧栏资产 Tab（本 fork 用独立素材库） |
| 生成后保留节点提示词输入（0.10） | 高（自用迭代改词） | 低：单文件 `canvas-node-prompt-panel` | **已移植** | 提交后不清空；仅换节点时重置 |
| 提示词搜索防抖（自用体验） | 中 | 低：输入即时、查询 280ms 防抖 | **已移植** | 提示词中心 + 画布/工作台 `PromptSelectDialog` 共用 `useDebouncedValue` |
| 提示词源统一 JSON / 自定义源 / 画布提示词 Tab（0.10） | 中 | 中：与本库提示词中心改造叠层 | 候选 | 确认不破坏现有库后再做 |
| 提示词源自定义脚本抓取（0.9） | 中 | 中：与本库提示词改造叠层 | 候选 | 确认不破坏现有库后再做 |
| Agent 任务状态 / 多标签页隔离 / 附件上画布（0.10） | 中（视 Codex 频率） | 中高：Agent 状态与画布桥 | 低优先 | 仅高频本地 Agent 时 |
| 画布提示词弹窗放大编辑 + 实时同步（0.15） | **高**（长提示词自用） | 低：面板/弹窗 UI，勿动生成 payload | **已移植** | Modal + 共用 prompt 状态；生成 payload 未改 |
| 画布多图合并为可展开单节点 + 失败重试/删/副本/单图下载 + 主图切换（0.15） | **高**（批量生图） | **中高**：与本 fork 并发生成「多子节点」模型交叉 | **部分移植** | **已做**：失败重试强制显示、副本（脱批）、单图下载/删除维护 batch；**不做**合并为单节点（本 fork 继续 root+子节点） |
| 画布选择/移动模式 + Ctrl/空格临时反转（0.15） | 中高 | 中：工具条/快捷键/焦点 | **已移植** | 默认 pan（保持空白拖平移）；Space 反转；Ctrl 强制框选 |
| 元素列表定位缩放上限 100%（0.15） | 中 | 低：节点列表居中逻辑 | **已移植** | `fitViewportScaleForNode` maxZoom=1；滚轮上限仍 5 |
| 画布一键排版（自用，非上游包） | 高（工作流可读） | 低：纯位置函数 | **已移植** | Kahn + barycenter 减交叉；垂直对齐；组/batch 为单元；有选中排选中否则全画布 |
| 多选成组 / 取消成组（自用，基于既有 Group 节点） | 高 | 低：metadata.groupId + 框尺寸 | **已移植** | 多选栏/右键；≥2 成员；拖组仍带动成员 |
| 关闭输入面板保留图/文编辑提示词；点空白收起工具条/面板（0.15） | 高（防丢词） | 低 | **部分已有** | 本 fork 已即时写回 `metadata.prompt`；空白收起可再对一下 |
| 配置页本地存储 / 站点配额 / 对象仓库占用（0.15） | 中 | 低～中：只读展示；勿与云 `usage/limits` 混淆 | 候选 | 可做本机 IndexedDB 体积摘要；云配额已有账号弹层 |
| Agent `/` 选 Skill + `@` 插画布素材 + 悬浮预览（0.15） | 中（视 Agent 频率） | 中高：与本 fork 工作台/画布 `@` 参考 chip 分轨 | 低优先 | 勿整包搬 Skill；本 fork 工作台 `@` 已落地 |
| Agent 用户消息元数据版本化文件存储（0.15） | 中 | 中：本地 Agent 存储 | 低优先 | 仅高频 Codex 丢附件时 |
| 清理本地图时保留工作台历史引用（0.15） | 中 | 中：与 image-storage / 工作台历史 | 候选 | 防误删预览 |
| 前端 i18n 中英 + 文档双语（0.14） | 低（自用中文） | 中：面广 | 后置 | 非主线 |
| Agent 本地 Codex Skill CRUD / 从对话生成草稿（0.14） | 中 | 高：Agent 状态/MCP | 观察 | 独立切片；勿与云/中转并行 |
| Agent 预热 / 日志结构化 / 命令组合并（0.13） | 低～中 | 中：仅 Agent 面板 | 低优先 | 高频排障时 |
| 节点缩放期暂停浮层工具条高频更新（0.13） | 中（防崩） | 低 | 候选 | 若缩放卡顿/崩溃再做 |
| Agent 选 Codex 模型与推理强度（0.12.1） | 中 | 低～中 | 部分重叠 | 本 fork 文本节点已有 `reasoningEffort`；Agent 侧按需 |
| 上游 MIT 协议更换（0.15.1） | 法务/发布 | 低：仅 LICENSE 文案 | **不自动跟** | 用户明确要求再改本 fork 协议 |
| 上游极简 compose/nginx | 无 | **禁止覆盖** | 拒绝 | — |
| GA/百度统计等 | 低 | 低 | 忽略 | 自用不需要 |

## 推荐移植顺序（减少返工）

```text
1. 本地 BYOK 体验小切片（透明背景 ✅ → 组节点 ✅ → 上游单文件 bugfix）
2. 中转兼容长期解（模型调用脚本 ✅ 最小切片；旧特判只维护不扩张）
3. 插件系统（独立分支 + 安全白名单；勿与计费/云同步并行大改）
4. 收费产品（开 API_PLATFORM_* + 支付入账）— 明确后置
5. Postgres / S3 — 与插件大改错开（P2 画布/素材云同步已完成）
```

## 切片作业清单（每次移植照抄）

1. `git fetch origin`；记下上游 commit/tag 与涉及路径。  
2. 新分支：`feat/upstream-<slice-name>`（基于当前 `a-hi1/main`）。  
3. 只改能力相关文件；**不碰** `docker-compose*.yml` / `nginx.conf` / 端口约定，除非切片本身就是部署修复。  
4. 本地验证：未登录 BYOK 生图/画布仍可用；若动云相关再跑 `check-cloud-stack` + `smoke-dual-track`。  
5. 更新：本矩阵状态、`pending-test.mdx`、必要时 `CLAUDE.md`。  
6. `git push a-hi1 <branch>` 或合入 `main` 后推 `a-hi1`。

## 与 P1 / P0.5c 的关系

| 线 | 目标 | 与上游关系 |
| --- | --- | --- |
| P0.5c | 双轨云历史可运维、可回归 | 无关上游画布扩展 |
| P1 底座（A～G） | 可选平台网关+账本代码，**默认关** | 自研；不依赖上游 |
| P1 收费产品 | 支付+默认扣积分体验 | **后置** |
| 上游切片 | 画布/中转扩展能力 | 本表 |

新会话接手：先读本文件 + `todo.mdx` + `git remote -v`，不要假设应整包升到上游版本号。
