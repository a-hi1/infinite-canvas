---
title: S0 人工验收清单
description: P0.5c 出门前仍须人点的最短清单与通过标准（配合自动脚本）
---

# S0 人工验收清单

自动脚本（本机 `3011`）通过后，用本清单收口。**通过 = 现象符合「期望」并勾选。**

前置：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\check-cloud-stack.ps1 -BaseUrl http://127.0.0.1:3011
powershell -ExecutionPolicy Bypass -File .\scripts\smoke-dual-track.ps1 -BaseUrl http://127.0.0.1:3011
```

硬失败须为 0。

## 出门状态

**本机自用 + 公网 B2：已出门**（A/B/C/D 均已人工勾选通过）。  
自动脚本：`check-cloud-stack` + `smoke-dual-track` 硬失败 0（本机 `3011`）。

## A. 双轨（发版门禁）

| # | 步骤 | 期望 | 勾选 |
| --- | --- | --- | --- |
| A1 | 未登录打开站点 | 可进首页/画布/素材/提示词，无强制登录墙 | [x] |
| A2 | 未登录生图 1 张（BYOK 已配） | 本机成功；可有「登录后可跨设备回看」类提示 | [x] |
| A3 | `docker compose stop api` 后刷新页面 | 页面仍 200；未登录仍可进画布/素材/提示词 | [x] |
| A4 | api 停时再试生图或只读本地历史 | 不因云失败变成「生成失败」 | [x] |
| A5 | `docker compose start api` | `/api/health` 恢复 | [x] |
| A6 | 注册/登录 | 成功；账号弹层有容量/条数/积分余额（可为 0） | [x] |
| A7 | 登录后生图 | 本机成功；云端 Tab 可见（出网允许时） | [x] |
| A8 | 断 api 或断网上云失败 | 本机记录仍为成功，可「重试上云」 | [x] |
| A9 | 同结果点两次重试上云 | 云端仍一条（deduped） | [x] |
| A10 | 用户 B 打开用户 A 的 `/api/files/:id` | 拒绝/404，不可读 | [x] |
| A11 | 人为弄失效会话后点云端操作 | 只提示一次登录失效，不连环报错 | [x] |

## B. 画布 / 素材同步（P2 代码已在，出门前点一轮）

| # | 步骤 | 期望 | 勾选 |
| --- | --- | --- | --- |
| B1 | 未登录新建画布节点 | 仅本机可用 | [x] |
| B2 | 登录后编辑画布约 2s | 有上云/已上云类状态；云失败不丢本地 | [x] |
| B3 | 删除画布后再同步 | 不复活（tombstone） | [x] |
| B4 | 素材增删后同步 | 删除不复活；未登录「仅本机」 | [x] |

## C. 近期创作面回归（防回归）

| # | 步骤 | 期望 | 勾选 |
| --- | --- | --- | --- |
| C1 | 提示词精选列表 | 前排多为有效预览；坏图占位 | [x] |
| C2 | 画布多选 ≥2 | 浮条可删/复制；Delete 可删 | [x] |
| C3 | 透明背景开关 | 可开可关并反映到请求（支持的渠道） | [x] |
| C4 | 导出当前/选中 | 能出 zip | [x] |
| C5 | `/lan-ai` 文生图（若在用 Grok2API） | 可选质量/比例映射；无参考图可出图 | [x] |

## D. 服务器 B2（给他人用的环境）

| # | 步骤 | 期望 | 勾选 |
| --- | --- | --- | --- |
| D1 | `git pull` + `compose up -d --build` | 服务 Up | [x] |
| D2 | 入口 `http://服务器:3001` | 无强制 3011 | [x] |
| D3 | `.env` 无本机 789x 代理（除非故意） | 卫生 | [x] |
| D4 | `./scripts/check-cloud-stack.sh` | 硬失败 0 | [x] |
| D5 | HTTPS 时 `API_COOKIE_SECURE=true` | Cookie 正常 | [x] |
| D6 | `./scripts/backup-api-data.sh` | 有压缩包 | [x] |
| D7 | 若用家里 Grok：frps + 家里 frpc | tunnel/lan-ai 为 401/200 | [x] |

## 出门判定

- **本机自用出门：** A 全勾 + B 关键项 + C 按你使用面勾选；自动脚本绿。  
- **公网给他人用：** 另加 D 全勾。  
- **仍禁止：** 未出门就开支付默认扣积分、整仓 merge 上游、大改插件 registry。

通过后在 [P0.5c 验稳收口](/docs/progress/p05c-acceptance) 勾对应项，并更新 G 节自动快照 tip。

**当前：已满足本机自用出门 + 公网 B2 出门。** 下一期见 [TODO](/docs/progress/todo)（S1 痛点 / 上游切片；收费仍后置）。
